# Plan 24 — Combat encounters

## Goal
DM creates encounters with participants (players auto-included; DM adds monsters from `monsters.json` or campaign customContent). Shared initiative tracker. Turn order. Monster HP/conditions managed by DM. `encounter-start`, `turn-start`, `monster-hp-change`, `encounter-end` events logged. Real-time sync.

## Context
Read `docs/DATA-MODEL.md` (encounter schema), `docs/EVENT-LOG.md`.

## Prerequisites
Plans 22-23 complete.

## Steps
- [x] 1. Route `/campaign/:id/encounter/:eid` → `<EncounterScreen />`. **24.2 livre la route LISTE `/campaigns/:cid/encounters` (`<EncountersListScreen>`) — convention `/campaigns/:cid/...` cohérente avec sessions, PAS le `/campaign/:id` littéral du plan.** **24.3 livre la route DÉTAIL `/campaigns/:cid/encounters/:eid` (`<EncounterScreen>`, tracker temps-réel) + rend les lignes de `EncountersListScreen` CLIQUABLES (navigation vers le détail).**
- [x] 2. DM dashboard "Créer rencontre" → modal: name, link to session, add monsters (search the monster DB, select, qty). **DÉCISION ADRIEN RÉSOLUE → option (a) SAISIE MANUELLE (nom + PV)** : `monsters.json` étant vide (0/332), la « recherche dans la DB monstres » est remplacée par une saisie manuelle de monstre (nom + PV, quantité ⇒ N participants numérotés « Gobelin 1/2… »), `monsterContentId: null`. **`link to session` NON câblé en 24.2** (la rencontre se crée hors séance, `sessionId: null` ; le lien viendra avec l'écran de combat 24.3 si utile). **CONSTAT BLOQUANT pour la « CA » du stopgap original** : le schéma participant documenté (DATA-MODEL.md) ne porte PAS de champ `ac` — la saisie de CA manuelle nécessiterait un changement de schéma (décision Adrien). Le tracker (initiative/tours/PV) fonctionne sans CA ; la CA viendra du stat block (bestiaire SRD) ou d'une décision schéma ultérieure. Livré 24.2.
- [x] 3. On create, write `encounters/{eid}` with status `'planned'`, participants array (all players auto-added; monsters per choice). **Livré 24.2** : `EncounterCreateModal` auto-inclut les fiches liées de la table (`useEncounterPartyDraft` — lecture cross-owner one-shot des PJ liés, snapshot des PV à la création) + les monstres manuels, puis appelle `createEncounter` (data layer 24.1). Auto-inclusion + expansion quantité + validations testées.
- [x] 4. **Roll initiative** button: rolls d20 + DEX mod for each participant in parallel. Players can re-roll if they want. Sorts participants by init desc. **Cœur livré 24.1** (`rollInitiativeFor` pur via `rollDieCrypto`, `applyInitiative` trie par init desc, `setParticipants` persiste). **Bouton « Lancer l'initiative » (jet de toute la table) + re-roll par participant livrés 24.3** — mod joueur résolu à la volée via la fiche liée (`resolveInitiativeModifiers`), monstre = 0. **DIVERGENCE step assumée : le re-roll côté JOUEUR (qui écrirait sa propre init sur le doc) est BLOQUÉ par la rule d'écriture `isDMOf` — il faudrait élargir la rule (décision Adrien). En V1 single-MJ le meneur lance/relance pour tout le monde.** Init verrouillée à `planned` (l'ordre se fige au démarrage, pas de re-tri mid-combat → pas de désync `turnIndex`).
- [x] 5. **Start encounter** → status `'active'`, `round=1`, `turnIndex=0`. Logs `encounter-start` event. **Data layer + logger livrés 24.1** ; **bouton « Démarrer le combat » + pose du pointeur `activeEncounterId` (+ campagne active, requise par l'event-logger) + `logEncounterStart` + `logTurnStart` du 1ᵉʳ participant livrés 24.3.** Garde-fous `another-encounter-active`/`no-participants` remontés en messages i18n.
- [x] 6. **Turn order strip** — horizontal scroll showing all participants in order. Active turn highlighted. "Fin du tour" advances `turnIndex`. Wraps round on overflow. **Logique de tour livrée 24.1** ; **strip horizontale (cartes nom + type + init + barre de PV, tour actif surligné) + bouton « Fin du tour » (→ `advanceTurn` + `logTurnStart` du nouveau participant actif) livrés 24.3.**
- [x] 7. **Monster control** (DM only): each monster card shows HP, conditions, AC. Tap to damage/heal/condition (logged as `monster-hp-change`, visibility 'all'). **Logique livrée 24.1** (`applyHpDelta` clampe 0..maxHp + renvoie before/after, `toggleCondition` add/remove idempotent ; `logMonsterHpChange`). **UI livrée 24.4** : chaque carte de monstre / PNJ (MJ-only) porte un bouton « PV / États » ouvrant `ParticipantControlModal` — dégâts/soin (montant saisi + boutons rapides ±1/5/10), journalisés `monster-hp-change` UNIQUEMENT si les PV changent réellement (delta absorbé au clamp = pas d'event) ; bascule des 15 états SRD ; les états actifs s'affichent en chips crimson sur la carte. Réservé aux participants `type !== 'player'` (un PJ se gère sur sa fiche, event `hp-change`). **NB visibilité** : `monster-hp-change` posé en **`dm`** (table EVENT-LOG.md), pas `all` comme l'écrit ce step — on suit la table. **CA toujours absente** (pas de champ `ac` sur le participant, constat 24.2). **États NON journalisés** : aucun kind `monster-condition-change` n'existe (EVENT-LOG.md) — l'état vit sur le doc partagé live ; un event dédié = changement de schéma d'événement (décision Adrien).
- [x] 7b. **Hand-off dégâts physiques (DM only)** : un panneau d'en-tête du tracker liste les `damage`/`attack` events récents en mode physique posés par les joueurs (« Lyralei — 11 tranchants · épée longue · Att 17 »). Le MJ tape un participant pour appliquer le total à ses PV (logged comme `monster-hp-change` ou `hp-change`), ou édite les PV manuellement. **Le joueur ne cible jamais ; le MJ choisit la cible.** Le total d'attaque s'affiche côté CA de la cible pour adjuger touché/raté à la main quand l'auto-crit/auto-raté n'a pas suffi (mode physique avec jet d'attaque ouvert). L'event reste dans le panneau jusqu'à application ou dismiss DM (TTL court, 5min, ou bouton « Ignorer »). **Livré 24.4 (steps 7b+8).** `EncounterHandoffPanel` MJ-only en combat : lit le feed d'events existant (`useCampaignEvents`, visibilité `all`/`dm` MJ) et FILTRE côté client (`deriveHandoffRows`) les `roll` physiques attack/damage récents (TTL 5 min) — **0 nouvel index, 0 nouvelle rule**. Lignes `damage` applicables (« Appliquer à… » → grille de cibles non-joueurs → tap = `applyParticipantHpDelta(−total)` + `logMonsterHpChange` via le chemin step 7, puis dismiss local) ; lignes `attack` informatives (« Att N » + « compare à la CA » — pas de champ `ac`). « Ignorer » + TTL retirent l'event. **Limite contenu (vérité du contenu) :** le `damageTypeLabel` (« tranchants ») n'est PAS journalisé dans le payload de l'event (vit seulement dans le toast, cf. `logRoll`) — la ligne affiche acteur · arme · total, **sans inventer** le type de dégâts. **Cible = non-joueurs uniquement** (un PJ se gère sur sa fiche, `hp-change` cross-owner — même frontière que step 7).
- [x] 8. **Party view** for players: shows all participants' HP bars (their own + party + monsters with DM-controlled visibility). **Livré 24.4 (steps 7b+8).** `EncounterPartyView` lisible par TOUS (joueurs + MJ), groupée « Votre groupe » (PJ) / « Adversaires » (monstres + PNJ), visible dans tous les statuts (≠ la strip d'init qui montre l'ORDRE). Réutilise `encounter-hp` (barre + ratio) + le résolveur de libellé d'état. **Visibilité PV monstres aux joueurs : NON masquée** (les joueurs lisent déjà `currentHp` via la rule `isMemberOf || isDMOf`) — un masquage nécessiterait un champ `hpVisible` par participant = **changement de schéma, décision Adrien (reportée)**. Dual-lens assumé : la strip (ordre + tour actif) et la party view (santé groupée) coexistent, comme un VTT (initiative tracker + party panel).
- [x] 9. **End encounter** → status `'completed'`, outcome selector (victory/defeat/fled). Logs `encounter-end`. **Data layer + logger livrés 24.1** ; **bouton « Clôturer le combat » + sélecteur d'issue inline (Victoire/Défaite/Fuite) + `logEncounterEnd` + libération du pointeur `activeEncounterId` livrés 24.3.** `outcome` toujours NON persisté sur le doc (vit dans l'event `encounter-end`, conforme EVENT-LOG.md).
- [x] 10. Real-time: all participants share state via Firestore listeners. **Livré 24.3** : `useEncounter` s'abonne en `onSnapshot` au doc rencontre (mirror `useMap`) — MJ et joueurs voient le même ordre d'init / round / tour actif / PV sans rafraîchir. L'écran est read-only sur la lecture ; toutes les écritures passent par `services/encounters.ts`.
- [ ] 11. Optional: encounter linked to a map (plan 27-30) — position field per participant. For S3, position is null.

### Tests
- [x] 12. e2e: DM creates 3-goblin encounter, rolls init, runs 2 rounds, kills all goblins, ends with victory. **Livré 24.5** : `tests/e2e/encounter-full-combat-uat.spec.ts` exécute le scénario d'acceptation LITTÉRAL bout en bout en une seule session (3 gobelins → init → 2 rounds RÉELS via 3 « Fin du tour » jusqu'au wrap Round 2 → kill ALL 3 gobelins via le contrôle MJ −10 PV → clôture Victoire). Passe par les VRAIES rules Firestore (create encounter + write PV + events MJ-only). Galerie `uat-review/jalon-24/24-full/` (01 préparée, 02 init, 03 Round 1, 04 Round 2, 05 tous morts 0/7, 06 victoire/Terminée). Les specs sœurs (lifecycle/monster-control/handoff-party) couvraient les briques ; celle-ci verrouille le parcours consolidé. **5/5 specs encounter e2e vertes** (non-régression).
- [x] 13. `pnpm typecheck && pnpm test && pnpm lint` — typecheck clean, **2450 fast tests verts**, lint clean. e2e encounter 5/5 verts contre l'émulateur.
- [x] 14. Commit: `feat(encounters): combat tracker (plan 24)` — livré en sous-commits 24.1→24.5 (le step 12 + clôture DoD = ce commit).

## Definition of Done
- [x] Create / start / run / end encounter works — créer (24.2) / lancer init + démarrer (24.3) / fin de tour + 2 rounds (24.3, e2e step 12) / clôturer + issue (24.3). Vérifié e2e bout en bout.
- [x] Players see live turn order and HP changes — `useEncounter` `onSnapshot` (24.3) : strip d'ordre temps-réel + `EncounterPartyView` PV groupés live (24.4 step 8), partagés MJ↔joueurs sans refresh.
- [x] DM controls monsters — `ParticipantControlModal` PV/états (24.4 step 7) + hand-off dégâts physiques (24.4 step 7b). MJ-only.
- [x] All events logged correctly — `encounter-start`/`turn-start`/`monster-hp-change`/`encounter-end` câblés (loggers 24.1, branchés 24.3+24.4), passent par les vraies rules de l'émulateur dans les e2e.

## Sous-jalons

### JALON 24.1 — Data layer (type + service + logger + rule-widening) ✅ livré
Fondation sans UI, entièrement testée. Mirror exact de 23.1 (sessions). Couvre le socle des steps 3/4/5/6/7/9 côté données + journalisation.

- **`src/shared/types/encounter.ts`** — `EncounterSchema` (Zod) reproduisant À L'IDENTIQUE la forme documentée `docs/DATA-MODEL.md` (aucun champ ajouté ⇒ pas de changement de schéma Firestore). `ENCOUNTER_STATUSES` (planned/active/completed/aborted), `PARTICIPANT_TYPES` (player/monster/npc), `ENCOUNTER_OUTCOMES` (victory/defeat/fled — exposé mais **PAS** dans `EncounterSchema` : l'issue vit dans l'event `encounter-end`, le doc ne la porte pas).
- **`src/shared/lib/services/encounters.ts`** — `createEncounter` (planned/round 0, normalise les participants, `currentHp` défaut = `maxHp`, `instanceId` auto si absent), `listEncounters`/`getEncounter`/`getActiveEncounter`, helpers PURS `rollInitiativeFor` (1d20+mod via `rollDieCrypto`) / `applyInitiative` (tri init desc, stable sur égalité) / `nextTurn` (wrap → round +1) / `applyHpDelta` (clamp 0..maxHp + before/after) / `toggleCondition` (add/remove idempotent), transitions `startEncounter` (garde-fous `no-participants` + `another-encounter-active`, re-start idempotent), `advanceTurn`, `endEncounter`, `setParticipants`. Pattern aligné sur `sessions.ts` (`trackPendingWrite`, `serverTimestamp`, `EncounterServiceError` typée). La séparation calcul pur / I/O rend tout testable sans émulateur.
- **`src/shared/lib/event-logger.ts`** — `logEncounterStart` / `logEncounterEnd` (issue dans le payload) / `logTurnStart` / `logMonsterHpChange` (visibilité **`dm`** par table EVENT-LOG.md, calcule `delta = after - before`). Tous `actorCharacterId: null` (actions MJ), `encounterId` explicite.
- **`src/shared/lib/slices/active-campaign-slice.ts`** — ajout `activeEncounterId` + `setActiveEncounter`. L'event-logger tague désormais `encounterId: input.encounterId ?? activeEncounterId` (mirror du `sessionId`). `clearActiveCampaign` libère les 3 pointeurs.
- **`firestore.rules`** — lecture encounters élargie `isMemberOf(cid)` → `isMemberOf(cid) || isDMOf(cid)`. Même gap MJ-pur que sessions 23.1 / events 22.3 (un MJ pur n'a pas de doc `members/` ⇒ ne pouvait pas lire SES PROPRES rencontres). **⚠️ Non déployé** — `pnpm firebase:deploy:rules` requis avant que l'UI 24.x ne consomme la liste en prod (cf. discipline deploy CLAUDE.md). Inerte localement (émulateur OK).
- **Tests** — `services/__tests__/encounters.test.ts` (25 : create/list/get/getActive, initiative pur, tour pur + advanceTurn, start avec 3 garde-fous, end sans outcome sur doc, applyHpDelta clamp haut/bas/introuvable, toggleCondition). `__tests__/event-logger.test.ts` (+4 : encounter-start/end/turn-start/monster-hp-change visibilité dm + delta). `slices/__tests__/active-campaign-slice.test.ts` (+2 : setActiveEncounter isolé, clear libère les 3). `tests/firestore-rules.test.ts` (+11 : bloc encounters read membre/MJ/non-membre incl. **gap MJ rouge-avant-vert prouvé** — 2 tests rouges sans `|| isDMOf`, vérifié — query liste, CRUD MJ-only) → 99/99 contre l'émulateur. Triple gate verte (typecheck + 2357 fast + lint) + `pnpm test:rules` 99/99.

**Décisions tactiques 24.1 :**
- Helpers d'initiative/tour/PV/états **purs** (sans I/O), séparés des fonctions Firestore — testables en isolation, déterministes (le d20 vient de `rollDieCrypto` mocké dans les tests). Les fonctions I/O lisent l'état (`getEncounter`) puis posent le résultat calculé.
- `outcome` NON ajouté au doc encounter (schéma documenté ne le porte pas) — l'issue vit dans l'event `encounter-end`. Ajout au doc = changement de schéma → décision Adrien.
- Garde-fou « une seule rencontre active » + « pas de participants » placés dans `startEncounter` (data layer, testable) — client-enforced comme `startSession` (les rules ne peuvent pas asserter l'unicité cross-doc à bas coût).
- `logMonsterHpChange` en visibilité **`dm`** (suit la table EVENT-LOG.md) alors que le step 7 du plan écrit `'all'` — divergence du step vs la table de référence ; on suit la table. À reconfirmer en UI 24.x si le MJ veut révéler les PV monstres.

### JALON 24.2 — Liste + création de rencontre (route + écran + modale) ✅ livré
Mirror exact de 23.2 (sessions). Couvre les steps 1 (partiel — route liste), 2 et 3.

- **`src/features/campaigns/use-encounters.ts`** — hook liste one-shot + refresh (mirror `useSessions`), consomme `listEncounters` (24.1) et la rule de read `isMemberOf || isDMOf`.
- **`src/features/campaigns/use-encounter-party-draft.ts`** — lecture cross-owner **one-shot** (`getDoc`, pas `onSnapshot`) des fiches liées de la table → participants joueurs (nom + PV courants/max, snapshot à la création). Extraction DÉFENSIVE des 3 champs (`name`, `hp.current`, `hp.max`) stables v1/v2 plutôt qu'un parse `CharacterSchema` complet (ne rejette pas une fiche v1 non migrée). `enabled` gate le fetch (la modale ne lit la table qu'à son ouverture). Une fiche illisible est exclue et lève `hadReadError`.
- **`src/features/campaigns/encounter-create-modal.tsx`** — modale « Nouvelle rencontre » : nom (obligatoire) + section joueurs auto-inclus (lecture seule) + saisie manuelle de monstres (nom + PV + quantité, lignes ajout/retrait). Quantité > 1 ⇒ N participants numérotés. Validations : nom requis/≤120, nom de monstre requis sur ligne active, PV monstre > 0, au moins 1 participant. Appelle `createEncounter`.
- **`src/features/campaigns/encounters-list-screen.tsx`** — route `/campaigns/:cid/encounters` (mirror `SessionsListScreen`) : liste (cartes statiques : nom + nb participants + chip statut), empty state MJ/membre, CTA « Créer une rencontre » MJ-only. Pluriel correct (« 1 participant » / « N participants »).
- **`src/routes.tsx`** + **`campaign-detail-screen.tsx`** — route déclarée ; entrée MJ « Rencontres » ajoutée à côté de « Séances » dans la nav du détail campagne.
- **i18n** — bloc `encounters.*` (FR + EN, ~45 clés) + `campaigns.detail.encountersCta`. Terminologie FR officielle : « Rencontre » (= encounter SRD FR), « Préparée/En cours/Terminée/Abandonnée » pour les 4 statuts.
- **Tests** — `use-encounters.test.tsx` (5, mirror sessions), `use-encounter-party-draft.test.tsx` (6 : extraction, fiche absente/malformée ⇒ hadReadError, enabled=false, vide, erreur globale), `encounters-list-screen.test.tsx` (11 : empty MJ/membre, identité des libellés de statut, pluriel singulier/pluriel rouge-avant-vert, erreur+retry, et flow modale : nom requis, joueurs auto-inclus → participants joueurs exacts, monstre qty=2 → 2 participants numérotés, PV manquant → erreur, aucun participant → erreur, erreur create générique). **+22 tests.** Triple gate verte (typecheck + 2379 fast + lint).
- **UAT e2e** — `tests/e2e/encounters-list-uat.spec.ts` (émulateur, vert) → galerie `uat-review/jalon-24/24.2/` (01 liste vide, 02 modale fullPage+viewport, 03 modale avec monstre, 04 liste, 05 mobile, 06 tablet). Non-régression vérifiée : `campaigns-detail-uat` + `sessions-list-uat` + `session-lifecycle-uat` verts après le changement de nav.

**Décisions tactiques 24.2 :**
- **Décision Adrien « monstres vides » tranchée option (a)** (saisie manuelle nom + PV). Le cœur du tracker (initiative/tours/PV/hand-off) ne dépend pas du stat block ; le bestiaire SRD enrichira plus tard.
- **CONSTAT (nouveau, à arbitrer plus tard) : pas de champ `ac` sur le participant** (DATA-MODEL.md ne le porte pas). La « CA » du stopgap original n'est donc PAS saisissable sans changement de schéma. Reportée : viendra du stat block monstre ou d'une décision schéma dédiée. Ne bloque pas le tracker.
- **Lien séance (`sessionId`) non câblé** à la création (rencontre hors séance, `null`). À reconsidérer en 24.3 si l'écran de combat veut rattacher la rencontre à la séance active.
- **PV joueurs figés à la création** (snapshot). La ré-synchronisation live fiche→rencontre est un sujet de l'écran de combat 24.x (real-time, step 10).
- **Lignes de liste statiques** (pas de navigation) tant que `EncounterScreen` (24.3) n'existe pas — pas de route morte.

### JALON 24.3 — Écran de combat (tracker temps-réel : initiative + tours + cycle de vie) ✅ livré
Couvre les steps 1 (route détail + lignes cliquables), 4 (initiative), 5 (start), 6 (turn order), 9 (end + issue), 10 (real-time).

- **`src/features/campaigns/use-encounter.ts`** — abonnement **temps-réel** `onSnapshot` au doc `campaigns/{cid}/encounters/{eid}` (mirror `useMap`, PAS le one-shot de `useSession` : une rencontre en cours est partagée live). Parse Zod `EncounterSchema`, gère doc absent (`encounter-not-found`), erreur de transport, parse invalide. Read-only.
- **`src/features/campaigns/resolve-initiative-modifiers.ts`** — résout le modificateur d'init de chaque participant AU JET, renvoyé en `Map<instanceId, modifier>`. Le participant (schéma 24.1) ne porte NI modificateur NI `ownerUid` : on joint `characterId` → roster (`LinkedMember`) → lecture cross-owner `users/{uid}/characters/{id}.initiative` (rule `gmCanReadLinkedCharacter`). Repli **0** (monstre, fiche introuvable/non liée, `initiative` non numérique) ; une lecture qui throw est isolée (les autres résolvent).
- **`src/features/campaigns/encounter-screen.tsx`** — route `/campaigns/:cid/encounters/:eid`. Header (nom + chip statut + « Round N » en actif). Contrôles **MJ-only** : `planned` → « Lancer l'initiative » (jet 1d20+mod de toute la table) + re-roll par participant + « Démarrer le combat » ; `active` → « Fin du tour » + « Clôturer le combat » avec sélecteur d'issue inline (Victoire/Défaite/Fuite). Strip d'ordre d'initiative horizontale : cartes (nom, badge Monstre, init, barre de PV teal/gold/crimson selon ratio), tour actif surligné (`aria-current` + glow doré). Loggers 24.1 câblés (`encounter-start` au démarrage, `turn-start` au démarrage + chaque fin de tour, `encounter-end` à la clôture) ; pointeurs `activeCampaignId`/`activeEncounterId` posés avant chaque écriture d'event (sinon `writeEvent` no-op), libération du pointeur de rencontre à la clôture.
- **`encounters-list-screen.tsx`** — `EncounterRow` rendue **cliquable** (bouton, mirror `SessionRow`) → navigue vers le tracker.
- **`routes.tsx`** — route détail déclarée AVANT la route liste (spécificité).
- **i18n** — bloc `encounters.detail.*` / `encounters.action.*` / `encounters.outcome.*` / `encounters.turnOrder.*` / `encounters.participant.*` (FR + EN, ~30 clés). Terminologie FR officielle : « Round » (SRD FR), « Initiative », « Victoire/Défaite/Fuite ».
- **Tests** — `resolve-initiative-modifiers.test.ts` (7 : joueur résolu, mod négatif, monstre 0, hors-roster 0, fiche absente 0, non-numérique 0, throw isolé), `use-encounter.test.tsx` (7 : gating, doc valide, not-found, transport error, parse invalide, unsub), `encounter-screen.test.tsx` (9 : boutons planned, roll → setParticipants init 10, start → loggers + pointeur, erreur another-active, fin de tour → turn-start du nouvel actif, clôture victoire → endEncounter + log + pointeur libéré, Round N + surlignage actif, joueur sans contrôle, not-found), +1 nav cliquable sur la liste. **+24 tests.** Triple gate verte (typecheck + **2403 fast** + lint).
- **UAT e2e** — `tests/e2e/encounter-lifecycle-uat.spec.ts` (émulateur, vert) : créer 3 gobelins → lancer init → démarrer → fin de tour → clôturer (victoire), passe par les VRAIES rules Firestore (create encounter + create events = MJ). Galerie `uat-review/jalon-24/24.3/` (01 préparée, 02 init lancée, 03 actif/round 1, 04 après fin de tour, 05 sélecteur d'issue, 06 terminée). Non-régression : 114 specs e2e vertes.

**Décisions tactiques 24.3 :**
- **Real-time `onSnapshot` (step 10) vs one-shot des sessions** : une rencontre EN COURS est l'état le plus « live » du jeu (tour partagé, PV) — d'où le listener, contrairement à `useSession`/`useEncounters` qui restent one-shot+refresh (catalogue à faible mutation).
- **Modificateur d'init résolu À LA VOLÉE** faute de champ stocké (schéma 24.1 figé) — jointure roster + lecture cross-owner. Repli 0 robuste.
- **Initiative verrouillée à `planned`** : pas de re-tri une fois le combat démarré (sinon `turnIndex` désynchronise l'ordre en cours).
- **Re-roll JOUEUR bloqué par la rule `isDMOf`** (écriture encounter MJ-only) → MJ lance/relance pour toute la table en V1. Un re-roll joueur écrivant sa propre init nécessiterait un élargissement de rule = **décision Adrien** (reportée).
- **Pointeur campagne active reposé sur l'écran MJ** : l'event-logger no-op sans `activeCampaignId` ; on (re)pose campagne + rencontre avant chaque log (robuste à un reload mid-combat), en préservant le pointeur de séance.

### JALON 24.4 (step 7) — Contrôle MJ des PV / états monstres ✅ livré
Premier sous-commit de 24.4. Branche l'UI de contrôle MJ sur les helpers purs (`applyHpDelta`/`toggleCondition`) + le logger (`logMonsterHpChange`) déjà livrés en 24.1. L'écran de combat 24.3 est le réceptacle.

- **`src/shared/lib/services/encounters.ts`** — 2 wrappers I/O read-then-write (mirror `advanceTurn`) : `applyParticipantHpDelta` (lit, compose `applyHpDelta` clamp 0..maxHp, pose, renvoie `{before, after}` pour le payload) et `setParticipantCondition` (lit, compose `toggleCondition`, pose). Non transactionnels — séquentialisés par l'UI (`actionPending`), suffisant V1 single-MJ.
- **`src/features/campaigns/encounter-hp.ts`** — extraction des helpers d'affichage PV (`HP_BAR_HEALTHY`/`HP_BAR_WOUNDED`, `hpBarColor`, `hpRatio`) hors de `encounter-screen.tsx`, partagés carte + modale (+ party view 24.4 step 8 à venir).
- **`src/features/campaigns/participant-control-modal.tsx`** — modale MJ d'un participant (présentationnelle pure, `conditions` en prop) via `DetailModal` : section PV (barre + montant saisi + boutons « − Dégâts »/« + Soin » + raccourcis ±1/5/10) et grille des 15 états SRD (bascule, `aria-pressed`). `localize(c.name)` pour les libellés.
- **`encounter-screen.tsx`** — bouton « PV / États » sur les cartes `type !== 'player'` (MJ-only) ouvrant la modale (cible dérivée EN LIVE du doc → PV/états se rafraîchissent via `onSnapshot`). `handleApplyHp` : `applyParticipantHpDelta` puis `logMonsterHpChange` **si `before !== after`** (delta absorbé au clamp = pas d'event), `ensurePointers` avant le log. `handleToggleCondition` : `setParticipantCondition` (pas d'event). Chips d'états actifs sur la carte (`useContent('conditions')` au niveau écran, résolveur de libellé partagé carte/modale).
- **i18n** — bloc `encounters.control.*` (FR + EN, ~10 clés). Terminologie 100 % réemploi projet (« PV », « États », « Dégâts », « Soin », « Montant ») — zéro nouveau terme D&D ⇒ pas d'arbitrage terminologique.
- **Tests** — `encounters.test.ts` (+4 : `applyParticipantHpDelta` clamp + before/after + isolation des autres participants, `setParticipantCondition` add/remove). `participant-control-modal.test.tsx` (7 : PV affichés, Dégâts→delta négatif, Soin→positif, raccourcis ±5, gating montant ≤ 0, toggle add/remove + `aria-pressed`, pending désactive tout). `encounter-screen.test.tsx` (+5 : bouton monstre vs pas joueur, ouverture modale → `applyParticipantHpDelta`+`logMonsterHpChange`+pointeur, pas de log si PV inchangés, toggle état → `setParticipantCondition`, chips d'états sur la carte ; +1 assertion no-control sur le test joueur). **+16 fast tests (2403 → 2419).** Triple gate verte (typecheck + 2419 fast + lint).
- **UAT e2e** — `tests/e2e/encounter-monster-control-uat.spec.ts` (émulateur) : créer 3 gobelins → init → démarrer → 10 dégâts (7→0, **kill**, partie du step 12) → état « Empoisonné » → carte reflète 0/7 + chip. Galerie `uat-review/jalon-24/24.4/` (01 tracker actif, 02 modale pleine+viewport, 03 dégâts, 04 état, 05 carte KO).

**Décisions tactiques 24.4 (step 7) :**
- **Contrôle réservé aux non-joueurs** (`type !== 'player'`) : le logger `monster-hp-change` est monstre-scoped ; les PV d'un PJ relèvent de sa fiche (event `hp-change`, écriture cross-owner). Évite toute complexité d'écriture sur le doc personnage.
- **États persistés sans event** : aucun kind `monster-condition-change` (EVENT-LOG.md). L'état vit sur le doc rencontre (partagé live). Créer un kind dédié = changement de schéma d'événement → **décision Adrien** (reportée). Le journal (plan 25) ne capturera donc pas les états monstres en V1 — gap mineur assumé.
- **Pas de log si PV inchangés** : un delta absorbé au plancher 0 / plafond maxHp laisse `before === after` → on n'émet pas d'event de bruit.
- **Visibilité PV monstres aux joueurs (step 8) reportée** : les joueurs lisent déjà `participants[].currentHp` via le doc (rule `isMemberOf || isDMOf`) → ils voient les PV monstres en live. Les masquer = champ `hpVisible` par participant = **changement de schéma → décision Adrien**, traité au step 8.

### JALON 24.4 (steps 7b + 8) — Hand-off dégâts physiques + vue de groupe ✅ livré
Second sous-commit de 24.4. Aucun changement de schéma / rule / index : step 7b filtre côté client le feed d'events existant, step 8 est de la présentation pure sur le doc déjà lisible.

- **`src/features/campaigns/encounter-handoff.ts`** — dérivation PURE `deriveHandoffRows(events, participants, dismissedIds, now, ttlMs)` : filtre les `roll` physiques attack/damage non-ignorés et dans la fenêtre TTL (`HANDOFF_TTL_MS` = 5 min), résout l'acteur via `actorCharacterId` → participant joueur de même `characterId` → `name` (repli `null` → libellé générique). Un `createdAt` serveur pas encore résolu (`null` local) = traité « tout frais » (inclus). Ordre du feed (createdAt desc) préservé.
- **`src/features/campaigns/encounter-handoff-panel.tsx`** — `EncounterHandoffPanel` (présentationnel, `<section aria-label>` = role region) : lignes `damage` (acteur · arme · « N dégâts » + « Appliquer à… » dépliant une grille de cibles + « Ignorer ») et `attack` (« Att N » + texte informatif « compare à la CA » + « Ignorer »). `pending` désactive tout.
- **`src/features/campaigns/encounter-party-view.tsx`** — `EncounterPartyView` (présentationnel, `<section aria-label>`) : groupe `player` → « Votre groupe », `monster`/`npc` → « Adversaires » ; chaque ligne = nom + PV courant/max + barre (réutilise `encounter-hp`) + chips d'états. Un groupe vide n'est pas rendu. Empty state si 0 participant.
- **`encounter-screen.tsx`** — abonnement au feed via `useCampaignEvents(isGm ? cid : undefined, { isDM: isGm })` (pas d'abonnement côté joueur ; le panneau est MJ-only). `dismissedHandoffIds` (Set local) pour les events appliqués/ignorés. Hand-off rendu MJ-only quand `isActive && rows>0` ; `handleApplyHandoff` réutilise `handleApplyHp(target, −total)` (chemin step 7) puis dismiss. `EncounterPartyView` rendu pour TOUS sous la strip d'init.
- **i18n** — blocs `encounters.handoff.*` (~11 clés) + `encounters.party.*` (~5 clés), FR + EN. Terminologie 100 % réemploi projet (« Att », « CA », « Dégâts », « Ignorer », « Cible », « Joueur », « Adversaires », « Votre groupe ») — zéro nouveau terme D&D ⇒ pas d'arbitrage terminologique.
- **Tests** — `encounter-handoff.test.ts` (13 : filtrage kind/mode/rollKind/total, TTL haut/bas/null, dismiss, résolution acteur null x2, ordre préservé). `encounter-handoff-panel.test.tsx` (7 : rendu dégâts, « Appliquer à… » révèle + applique `(eventId,total,instanceId)`, « Ignorer », ligne attaque informative sans apply, acteur null → « Joueur », pending désactive, sans cible → message). `encounter-party-view.test.tsx` (5 : groupage allies/enemies sans fuite, PV exacts, chips d'états, groupe vide non rendu, empty state). `encounter-screen.test.tsx` (+6 : panneau hand-off MJ + acteur résolu, application → `applyParticipantHpDelta(−total)` + `logMonsterHpChange` + retrait, « Ignorer » sans application, joueur sans panneau, pas de panneau hors combat ; party view joueur groupée + PV exacts). **+31 fast tests (2419 → 2450).** Triple gate verte (typecheck + 2450 fast + lint).
- **UAT e2e** — `tests/e2e/encounter-handoff-party-uat.spec.ts` (émulateur) : seed (Admin SDK, nouveau helper `seedEncounter`) d'une rencontre active riche (1 PJ allié + 3 gobelins PV variés) → vue de groupe 2 colonnes → seed de 2 events `roll` physiques (dégâts 11 + attaque 17) → panneau hand-off → « Appliquer à… » → Gobelin 1 (5 → 0). Galerie `uat-review/jalon-24/24.4-suite/` (01 combat+groupe, 02 panneau hand-off, 03 cibles, 04 appliqué).

**Décisions tactiques 24.4 (steps 7b + 8) :**
- **Filtrage côté client du feed existant** (pas de query dédiée ni d'index) : le payload (`mode`, `rollKind`) n'est pas requêtable à bas coût (champ map imbriqué) et la query visibilité est déjà la barrière de sécurité. `useCampaignEvents` (limit 20) suffit pour une fenêtre de 5 min en combat.
- **`damageTypeLabel` non journalisé** : assumé. La ligne montre acteur · arme · total sans inventer le type de dégâts (politique « vérité du contenu »). Si un jour le type est ajouté au payload de `logRoll`, la ligne pourra l'afficher.
- **Cible du hand-off = non-joueurs** : appliquer sur un PJ écrirait sa fiche en cross-owner (`hp-change`) — même frontière que step 7. Reporté avec le reste du contrôle PJ.
- **Party view = dual-lens, pas un doublon** : la strip montre l'ORDRE de combat + le tour actif ; la party view montre la SANTÉ groupée, visible même en préparation. Coexistence courante en VTT. Léger recouvrement de l'info PV assumé — Adrien arbitre en UAT.
- **PV monstres visibles aux joueurs** : déjà lus via la rule de lecture du doc. Les masquer = champ `hpVisible` = changement de schéma → décision Adrien (reportée).

### JALON 24.5 — e2e complet (step 12) + clôture DoD ✅ livré
Dernier sous-commit de 24. Aucun code applicatif neuf : un seul spec e2e consolidé qui exécute le scénario d'acceptation littéral du step 12 bout en bout, + cochage de la Definition of Done.

- **`tests/e2e/encounter-full-combat-uat.spec.ts`** — créer une rencontre 3 gobelins (7 PV) → lancer l'initiative → démarrer → faire DEUX rounds réels (3 « Fin du tour » → wrap Round 2, assertion `Round 2` visible) → tuer les 3 gobelins via le contrôle MJ (helper `killGoblin` : −10 PV → 0/7, fermer) → assertion des 3 cartes à 0/7 → clôturer sur « Victoire » → assertion statut « Terminée » + disparition de « Fin du tour ». Passe par les VRAIES rules Firestore de l'émulateur (create encounter + write PV + events MJ-only). Galerie `uat-review/jalon-24/24-full/` (01 préparée, 02 init, 03 Round 1, 04 Round 2, 05 tous morts, 06 victoire).
- **Non-régression** — 5/5 specs encounter e2e vertes ensemble (`encounter` filter : full-combat + lifecycle + monster-control + handoff-party + list). Triple gate : typecheck clean, **2450 fast** verts, lint clean.

**Décision tactique 24.5 :** un spec consolidé DISTINCT plutôt que d'étendre `encounter-lifecycle-uat` — le step 12 demande explicitement « 2 rounds + kill all + victory » en un seul parcours, que ni le lifecycle (pas de kill, 1 fin de tour) ni le monster-control (kill 1, pas de 2 rounds ni de clôture) ne couvraient bout en bout. Le spec dédié = preuve d'acceptation lisible + galerie UAT propre pour la signature finale du plan.

### Reste à livrer (UI + décisions Adrien requises)

**✅ DÉCISION ADRIEN RÉSOLUE (2026-06-24) — `monsters.json` VIDE → option (a) saisie manuelle, livrée en 24.2.**
La création de rencontre fonctionne par saisie manuelle de monstre (nom + PV). Le SRD-sourcing du bestiaire reste un plan dédié à venir ; il enrichira `monsterContentId` a posteriori. **Nouveau constat reporté** : le champ `ac` n'existe pas sur le participant (DATA-MODEL.md) — saisie de CA manuelle = changement de schéma, à arbitrer si besoin. Historique de la décision ci-dessous :
`public/data/monsters.json` est `[]` (0/332, statut CRITIQUE/bloquant S3 documenté dans `docs/AUDIT-SRD-COMPLETUDE.md`). La « recherche dans la DB monstres » (step 2) n'a aucune donnée. Le `MonsterSchema` existe (`src/shared/types/content.ts`) mais le bestiaire n'a jamais été peuplé — son SRD-sourcing est un **plan dédié à venir** (cf. decision log « pass-through monsters » + DEBT D-monsters). Le data layer 24.1 modélise `participants[].monsterContentId` sans pouvoir le résoudre. **Options pour l'UI 24.x** :
  - (a) **Stopgap saisie manuelle** : le MJ ajoute un monstre par nom + PV + CA libres (`monsterContentId: null`), sans stat block complet. Débloque tout le tracker immédiatement, le bestiaire SRD enrichira plus tard.
  - (b) **Attendre le plan bestiaire SRD** : ne pas livrer la création de rencontre tant que `monsters.json` n'est pas peuplé (bloque tout le plan 24 UI).
  Recommandation : **(a)** — le cœur du tracker (initiative, tours, PV, dégâts physiques hand-off) ne dépend pas du stat block complet ; la saisie manuelle suffit pour jouer, le bestiaire est un confort. **À trancher avant l'UI 24.2.**

Autres restes (→ **JALON 24.4**, l'écran de combat existe désormais et accueille ces contrôles) :
- ~~**Step 7 — contrôle des PV monstres (MJ)**~~ ✅ **livré en 24.4 (step 7)** : modale « PV / États », dégâts/soin → `logMonsterHpChange`, bascule d'états. **Visibilité PV monstres aux joueurs reportée au step 8** (nécessite un champ `hpVisible` = changement de schéma, décision Adrien).
- **Step 7b — hand-off dégâts physiques** : panneau MJ listant les `damage`/`attack` events récents en mode physique, application sur un participant ciblé par le MJ.
- **Step 8 — party view joueur** : barres de PV de toute la table avec visibilité contrôlée par le MJ (masquage des PV monstres).
- **Step 12 — e2e complet** : 3 gobelins, roll init, 2 rounds, **kill all** (dépend du step 7), victoire. 24.3 livre déjà créer→init→fin de tour→clôture victoire (sans le kill all).
- **Step 11** — position carte par participant (plan 27-30, S4).

## Notes for next plan
- Plan 25 (journal) uses encounter events heavily — encounter-start/end frame "Combat" sections in the journal. Les 4 loggers (`logEncounterStart`/`logEncounterEnd`/`logTurnStart`/`logMonsterHpChange`) sont en place depuis 24.1 ; le compilateur lira ces events.
- **⚠️ Rule `firestore.rules` NON déployée à déployer avant l'UI prod** : encounters read `|| isDMOf` (24.1). Couverte par `tests/firestore-rules.test.ts` (99/99 émulateur). S'ajoute aux 2 rules sessions/events déjà en attente (cf. plan 23 notes).
- **Blocage monstres** tranché option (a) en 24.2 (saisie manuelle). Plus de blocage sur la création.
- **JALON 24.3 ✅ livré = écran de combat** `EncounterScreen` : initiative (jet table + re-roll MJ), start/end + pose/libération `activeEncounterId`, turn-order strip + fin de tour, real-time `onSnapshot`, lignes de liste cliquables, 3 loggers câblés (`encounter-start`/`turn-start`/`encounter-end`). Le 4ᵉ logger (`logMonsterHpChange`) attend le contrôle des PV monstres (step 7 → 24.4).
- **JALON 24.4 (à venir) = contrôle monstres + party view** : steps 7 (PV/états monstres + `logMonsterHpChange`), 7b (hand-off dégâts physiques), 8 (party view joueur + visibilité PV monstres). L'écran de combat 24.3 est le réceptacle de ces contrôles.
- **⚠️ Re-roll JOUEUR (rule)** : le re-roll d'init côté joueur écrirait sa propre `initiative` sur le doc encounter, bloqué par la rule `create/update: isDMOf`. L'autoriser = élargir la rule (un joueur ne peut modifier QUE son propre participant) → **décision Adrien + changement de rule**. En V1 le MJ lance/relance pour tout le monde (réaliste en présentiel single-MJ).
- **CA non affichée au combat** : le participant ne porte pas de champ `ac` (constat 24.2). La CA viendra du stat block (bestiaire SRD) ou d'une décision schéma dédiée — non bloquant pour le tracker.
- **Init modifier résolu À LA VOLÉE en 24.3** (`resolveInitiativeModifiers`) : jointure `characterId` → roster → fiche liée `character.initiative`, monstre = 0. Pas de champ stocké sur le participant.
- Le hand-off dégâts physiques s'enrichira en S4 avec le ciblage visuel sur la carte (tokens, plan 30) : tap une cible sur la carte pour appliquer un événement de dégâts en cours. Le cœur « MJ applique sur un participant d'encounter » reste celui-ci.
