# Plan 12.5 — Dice mode (physical)

## Goal

Couche **mode physique** par-dessus le moteur digital de plan 12. L'app indique quoi lancer IRL (« Lance 1d20 — Attaque à l'épée longue »), le joueur saisit la/les **face(s) brute(s)** ; l'app applique les modificateurs, détecte nat 20/1, gère avantage/désavantage, calcule le total. La saisie est **optionnelle** : le joueur peut **« Passer »** et fermer sans rien logger (le slot/coût de sort, déjà consommé au moment du cast, reste consommé — c'est le chemin intentionnel et non-erreur).

Le pivot `rollWithFlags` devient mode-aware : il branche sur `effectiveDiceMode(user, campaign)` (campagne = `null` en S1). Le retour devient `RollResult | null` (null si le joueur « Passe »). Les 8 call sites de la fiche ajoutent un null-guard d'une ligne. La séquence attaque → dégâts est encapsulée dans `rollAttackDamage` qui gère le gate **Touché / Raté** (auto-crit si face 20, auto-raté si face 1, sinon choix manuel) et le prompt de dégâts **avec dés doublés en cas de crit** (modificateur non doublé, conforme SRD 5e).

Un toggle minimal **Digital / Physique** est exposé dans le header du `<RollHistoryPanel />` (le panel est déjà ouvrable depuis la fiche dès plan 12, étape 24). Plan 35 (S5) déplacera ce toggle dans le full settings screen + ajoutera `followCampaignDiceMode`.

**Hors scope** : le hand-off MJ (réception du total de dégâts physique côté DM, sélection de cible, application) — implémenté plan 24 (encounters) car il dépend du combat tracker + de l'existence d'une campagne (S2+).

## Context

- `CLAUDE.md > Decision log > Dice mode` : décision actée.
- `docs/DATA-MODEL.md > Dice mode resolution` : helper `effectiveDiceMode` documenté.
- `docs/DATA-MODEL.md > users/{uid}.settings` : champs `diceMode`, `followCampaignDiceMode` documentés.
- `docs/EVENT-LOG.md` : payload `roll` portera `mode + rawFaces` (effectif plan 22).
- `plans/12-dice-engine.md` : moteur digital + pivot + 8 call sites déjà migrés. `RollResult.mode` existe déjà (forcé `'digital'`) → ici on l'élargit.

## Prerequisites

Plan 12 livré et UAT validé. Le pivot `rollWithFlags` vit déjà dans `src/features/dice/`, les 8 call sites sont routés. Le shape `RollResult` est prêt à recevoir `mode: 'physical'`.

## Steps

### Helper de mode + settings user

- [x] 1. `src/shared/lib/rules/dice-mode.ts` livré : `DiceMode` re-exporté depuis `dice/types.ts` (proche du shape `RollResult`), `effectiveDiceMode(user, campaign|null)` implémente la règle DATA-MODEL.md.
- [x] 2. 4 tests dans `dice-mode.test.ts` : pas de campagne / follow=true / follow=false / défaut.
- [x] 3. `UserDoc` n'existait pas (le doc top-level `users/{uid}` n'est posé par aucun plan antérieur — plan 03 monte juste l'auth, plan 04 écrit `users/{uid}/characters/{id}`). En plan 12.5 j'introduis le doc avec `settings: { diceMode, followCampaignDiceMode }` peuplé à la première écriture via `setDoc(... { merge: true })`. Migration lazy : `setFromFirestore` applique les défauts si fields absents, donc la première lecture d'un doc existant sans `settings` reste valide.
- [x] 4. **Slice dédié** `src/shared/lib/slices/user-settings-slice.ts` (autonomie tactique : pas dans `auth-slice` pour garder l'auth slice limité au miroir Firebase Auth ; les settings vont grossir plan 35 avec locale/theme/notifications). Setter `setDiceMode(uid, next)` patch optimistic + Firestore merge. Hydratation via `subscribeToUserSettings(uid)` monté dans `auth-provider`.

### Élargissement du shape `RollResult`

- [x] 5. `DiceMode` élargi à `'digital' | 'physical'` dans `dice/types.ts`. Aucun call site existant ne casse. Le `RollResult` mode physique est construit dans `buildPhysicalResult` (exporté depuis `roll-with-flags.ts`) à partir des `rawFaces` saisies, avec `applyKeep` pour les advantages.

### `<PhysicalRollModal />` + ui-modals-slice

- [x] 6. `src/shared/lib/slices/ui-modals-slice.ts` livré : `pendingPhysicalRoll` + `requestPhysicalRoll(spec): Promise<{ rawFaces } | null>` + `resolvePhysicalRoll(...)`. Ajout en bonus `pendingHitMissGate` + `requestHitMissGate` pour le gate Touché/Raté en physique (étape 10). Auto-passe une requête précédente non résolue (anti-deadlock). Pas d'event bus.
- [x] 7. `src/features/dice/physical-roll-modal.tsx` livré : monté dans `App.tsx`, prompt par dé avec label `d{sides}`, validation stricte (rouge si hors plage, Valider désactivé tant que tout n'est pas valide), gestion avantage/désavantage 2d20kh1/kl1 (badge « Gardé » visuel sur la face conservée), total live + crit/fumble live, focus auto sur premier input, **Esc = Passer**. Wording « Passer » (pas « Annuler »).

### Pivot `rollWithFlags` mode-aware

- [x] 8. `rollWithFlags` mode-aware livré. Digital inchangé. Physique : ouvre PhysicalRollModal avec spec construit après application exhaustion+inspiration (donc 2d20kh1 si inspiration). Validation → `buildPhysicalResult` + toast + log + persist + consommation inspiration. Passer → `null` sans aucun side-effect (inspiration intacte).
- [x] 9. Signature `rollWithFlags` : `Promise<RollResult | null>` (idem `useDice` API). 8 call sites refactor pour guard sur `null`.

### Séquence attaque → dégâts mode-aware

- [x] 10. `rollAttackDamage` mode-aware livré. Digital : chain auto (plan 12 inchangé). Physique :
    - Attaque via `rollWithFlags` (silent). `null` → `{attack:null, damage:null}` sans gate.
    - Face 20 → auto-Touché + crit, skip gate.
    - Face 1 → auto-Raté + fumble, skip gate, pas de dégâts.
    - Face neutre → `<HitMissGateModal />` (gate dédié dans `ui-modals-slice`, décision tactique : modal léger séparé pour découpler du PhysicalRollModal). Passer = abort.
    - Si Touché → `rollDamageWithMode` avec `crit: true` si applicable (forceCrit ou face 20).
    - Toast combiné « Att X → Dgt Y » à la fin, ou message intermédiaire si Passer côté dégâts (« Att X · Dégâts passés »).

### `rollDamageWithMode` mode-aware

- [x] 11. `rollDamageWithMode` mode-aware. Digital : roller (inchangé, clamp à 0). Physique : prompt physique avec `finalAst.terms` (dés doublés sur crit), label suffixé « (crit · dés ×2) » pour transparence. Retour `RollResult | null`.

### Null-guards sur les 8 call sites (1 ligne / site)

- [x] 12. **Combat** (3 sites) : `attacks-list` (note inline — aucune action post-roll dépendante), `battle-hud` (null-guard explicite), `death-saves-modal` (null-guard explicite — l'état deathSaves reste inchangé si Passer).
- [x] 13. **Magie** (2 sites) :
    - `handleCast` : Option A documentée inline — slot consommé **avant** prompt damage ; si damage Passé (`null`), slot reste consommé, pas de toast dupliqué.
    - `handleAttackRoll` : null-guard explicite.
- [x] 14. **Essence** (3 sites) : `hexagram`, `saves-row`, `skills-list` — null-guard explicite. Pas d'autre side-effect.

### Toggle UI minimal S1 (Digital / Physique)

- [x] 15. Segment 2 options dans le header du `<RollHistoryPanel />` (composant `DiceModeToggle`), tooltips FR conformes à la spec. `setDiceMode(uid, next)` patch Firestore. Toggle désactivé tant que `hydrated === false` ou que l'utilisateur n'est pas auth (sécurité). Plan 35 reprendra le toggle + ajoutera `followCampaignDiceMode`.

### Persistance Dexie

- [x] 16. Pas de nouvelle migration — Dexie v2 a déjà les champs. Les rolls physiques persistent via `persistRollHistory` avec `mode: 'physical'` + rawFaces saisies. Le `<RollHistoryPanel />` `ModeBadge` lit `row.mode === 'physical'` → label `P` (déjà branché).

### Visual

- [x] 17. Badge `mode` D/P déjà branché dans `RollHistoryPanel` depuis plan 12 (`badgeLabel = row.mode === 'physical' ? 'P' : 'D'`). Plan 12.5 active la branche P en alimentant `mode: 'physical'` côté écriture. Pas de nouveau composant — le badge est dans l'historique, pas dans le toast (le toast `kind` suffit : crit/fumble/roll/damage).

### Tests

- [x] 18. `dice-mode.test.ts` : 4 tests effectiveDiceMode (pas de campagne / follow=true / follow=false / défaut).
- [x] 19. `roll-with-flags-physical.test.ts` : 8 tests — spec d20+mod, crit nat 20, fumble nat 1, advantage kh1, disadvantage kl1, Passer null + inspiration intacte, inspiration consommée seulement sur validation, exhaustion appliquée au modificateur.
- [x] 20. `use-dice-physical.test.ts > rollAttackDamage physique` : 4 tests — face 20 auto-Touché+crit+dés doublés, face 1 auto-Raté+pas de damage prompt, face neutre gate Touché→prompt non doublé, Passer attaque → ni attack ni damage. Bonus : face neutre gate Raté → pas de damage.
- [x] 21. `use-dice-physical.test.ts > rollDamageWithMode physique` : 3 tests — validation+clamp à 0, Passer null, crit doublé dés non modificateur.
- [x] 22. **Déviation tactique** : pas de tests dédiés par call site — chaque call site appelle le pivot via `rollWithFlags`/`rollAttackDamage`, et le contrat null-guard est entièrement testé au niveau du pivot. Tester côté call site dupliquerait sans valeur. Le compilateur garantit que les call sites traitent `RollResult | null` (typage strict, vérifié par triple gate). UAT physique d'Adrien validera le comportement E2E.
- [x] 23. `user-settings-slice.test.ts` : 3 tests — défaut digital+follow=true, `setDiceMode` optimistic+setDoc merge, `setFromFirestore` applique défauts sur champs absents.
- [-] 24. **DEFERRED** : e2e Playwright reporté plan 13.5 avec la dette e2e cumulée (cf. table dans plan 09). Le UAT physique d'Adrien couvre le path E2E manuellement.

### Final

- [x] 25. `pnpm typecheck && pnpm test && pnpm lint` — triple gate verte (199 tests passants, +23 vs plan 12 = +4 dice-mode +8 rollWithFlags-physical +8 use-dice-physical +3 user-settings, typecheck strict propre, lint propre).
- [x] 26. Commit : `feat(dice): physical mode + PhysicalRollModal + toggle (plan 12.5)`

## Definition of Done

- [x] `effectiveDiceMode` testé sur les 4 cas.
- [x] `users/{uid}.settings.{diceMode, followCampaignDiceMode}` ajoutés au schéma user + défauts + migration lazy.
- [x] `<PhysicalRollModal />` ouvre, valide la plage, calcule le total, gère « Passer » sans side effects.
- [x] Séquence attaque → dégâts en physique : gate Touché/Raté (auto-crit/auto-fumble selon face 20/1) + crit doublant les dés (modificateur non doublé).
- [x] Avantage/désavantage en physique : 2 saisies, garde la bonne (max ou min selon spec).
- [x] Slot de sort consommé avant prompt de dégâts ; « Passer » au prompt n'annule pas le slot (Option A — documenté inline dans `spell-detail-modal.tsx`).
- [x] Wording « Passer » (pas « Annuler ») sur le bouton de sortie.
- [x] Toggle Digital/Physique accessible dans le header `<RollHistoryPanel />`.
- [x] Tous les call sites de la fiche null-safe sur retour `RollResult | null`.
- [x] Badge `mode` (D / P) visible dans `<RollHistoryPanel />` (décision tactique : pas dans le toast, le kind crit/fumble/roll/damage suffit en feedback éphémère).
- [x] Historique Dexie persiste avec `mode: 'physical' + rawFaces` pour les jets physiques.
- [x] `pnpm typecheck && pnpm test && pnpm lint` verts.

## Notes for next plan

- **Plan 11 (radial FAB)** : les wedges qui produisent un jet doivent router via `useDice()` / `rollWithFlags` — déjà acté plan 12 (suppression du stub `dice.ts`). En mode physique le `<PhysicalRollModal />` s'ouvre automatiquement, aucune logique mode-specific côté wedge.
- **Plan 14 (campaigns model)** : `campaigns/{id}.settings.diceMode` + `effectiveDiceMode` reçoit alors un `campaign` non-nul. Le helper est déjà mode-aware sur ce paramètre — pas de retouche moteur, juste câbler `useActiveCampaign()` au pivot.
- **Plan 22 (event log)** : `logRollIfCampaign` câblé sur le vrai `event-logger.ts`. Le payload portera `mode + rawFaces + total + crit + fumble + advantage` — les jets physiques sont des événements de plein droit.
- **Plan 24 (encounters)** : hand-off MJ. Quand un joueur en campagne fait un jet de dégâts physique, le total remonte dans le tracker DM ; le MJ tape une créature pour appliquer ou édite ses PV à la main. Le joueur ne cible jamais.
- **Plan 35 (settings S5)** : full settings screen déplace le toggle ; ajoute `followCampaignDiceMode` (permettre à un joueur de décrocher du défaut de table).
- **Dette UX** : le gate « Touché/Raté » en physique sera enrichi plan 14/24 avec le seuil CA cible (auto-décision si CA connue). Aujourd'hui c'est manuel sauf face 20/1.
