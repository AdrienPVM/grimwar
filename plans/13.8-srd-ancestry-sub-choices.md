# Plan 13.8 — Sous-choix d'ascendance niveau 1 (Drakéide / Tieffelin / Elfe / Gnome / Goliath / Humain)

> **Plan B du jalon 1 « complétude SRD 5.2.1 niveau 1 ».** Consomme 13.7 (schema v2 + `public/data/ancestries.json` enrichi). 13.9 et 13.10 peuvent être livrés en parallèle de 13.8 une fois 13.7 mergé ; on garde la séquentialité par défaut pour la lisibilité des reviews.

## Goal

Faire en sorte que le wizard de création de personnage **impose** au joueur tous les sous-choix d'ascendance que le SRD 5.2.1 demande au niveau 1, avec un **pattern pédagogique débutant** (panneau d'aide par sous-choix, vocabulaire FR sans jargon, exemples concrets), et faire en sorte que la fiche **affiche correctement** les effets des sous-choix dans les modes existants (Combat pour le souffle Drakéide, Magie pour les sorts d'héritage Tieffelin et les cantrips de lignage Elfe/Gnome).

**Schéma cible (figé 13.7 §0.1)** : sous-objet `character.ancestrySubChoices` discriminé sur `ancestryId`. Les sous-choix vivent **groupés** dans ce sous-objet, pas à plat à la racine. Le wizard écrit dans `ancestrySubChoices.dragonAncestry` etc., la fiche lit depuis le même chemin.

**Critère d'utilité** : à la fin du plan, créer un Drakéide via le wizard force le joueur à choisir un type de dragon ; le souffle dans le mode Combat affiche le bon type de dégâts et la résistance correspondante ; le sort `breathWeaponDamageType` est utilisable par le moteur de dés. Un Tieffelin Infernal a `Fire Bolt` dans son mode Magie avec la caractéristique d'incantation choisie. Un Elfe Drow voit `Dancing Lights` + `Faerie Fire` (L3) + `Darkness` (L5) inscrits dans `knownSpells` / sorts d'héritage. Aucune ascendance avec sous-choix n'est créable sans poser ces choix.

**Rétro-compat** : les fiches `schemaVersion: 1` migrées par 13.7 portent `ancestrySubChoices: {}` (sentinelle vide). Aucun step de rattrapage UI n'est injecté dans le wizard pour compléter ces vieilles fiches — c'est une décision Adrien 13.7 §0.2 (les rares fiches v1 sont supprimées et recréées à neuf). La fiche **doit tolérer la sentinelle vide** sans planter (responsabilité 13.7 step 5).

## Context

**Lis avant de coder** :

- `docs/AUDIT-SRD-COMPLETUDE.md` — section A (tableau ascendance × sous-choix) et A.2 (listes exactes).
- `plans/13.7-srd-schema-and-extraction.md` — schema v2 + valeurs admises figés ; ce plan ne réinvente RIEN. En particulier §0.1 figeait : sous-objet `ancestrySubChoices` groupé + discriminated union sur `ancestryId`.
- `src/features/wizard/steps/ancestry-step.tsx` — pattern d'embranchement actuel (le composant a déjà la machinerie pour `subancestryId` retirée à 13.7 — ce plan refait l'UI proprement par sous-choix).
- `src/features/wizard/steps/spells-step.tsx` — exemple de step conditionnel rendu seulement si la classe est spellcaster. Pattern à reproduire pour les sous-choix d'ascendance.
- `src/features/wizard/help/ancestry-help.ts` — exemple de contenu pédagogique par ascendance. Le pattern « 1 file `*-help.ts` par sous-choix » est validé par Adrien.
- `src/features/wizard/submit-from-wizard.ts` — point d'écriture Firestore. Doit consommer les nouveaux champs schéma v2.
- `src/features/wizard/wizard-validation.ts` — validation pas-à-pas. Étendre pour bloquer l'avance tant que les sous-choix obligatoires ne sont pas posés.
- `src/features/sheet/modes/combat/` — où le souffle Drakéide doit s'afficher (battle HUD, attaques).
- `src/features/sheet/modes/magie/` — où les sorts d'héritage Tieffelin et cantrips de lignage Elfe/Gnome doivent apparaître.
- `src/shared/components/form/` — form-kit canonique (cf. plan 05). Tout `<Select>` / `<RadioCard>` passe par ce kit. **Aucun `<input>` natif** dans ce plan.
- `tests/e2e/seed-character.ts` — fixture Admin SDK pour seeds Firestore en e2e (livré au complément plan 13.5). À étendre avec presets `dragonbornL1Red`, `tieflingL1Infernal`, etc.
- `plans/DEBT.md > D5` — si les rules ont été durcies en 13.7 option (a), le deploy doit avoir eu lieu avant ce plan.

## Prerequisites

- Plan 13.7 livré + mergé + validé. `Character` v2 en place dans le code et la prod Firestore.
- Triple gate verte au démarrage.
- `public/data/ancestries.json` régénéré avec `options[]` par trait à sous-choix.
- Java/JRE 11+ pour les e2e émulateur ; `wizard-modal.spec.ts` continue de tourner sans Java (UI-only).

## Steps

### Composants form-kit & utilities (commit 1)

- [x] 1. `<RadioCardGroup>` livré dans `src/shared/components/form/radio-card-group.tsx`. fieldset + legend, role=radiogroup, name partagé pour nav flèches native, tap target ≥68px. 12 tests a11y + lisibilité.
- [x] 2. `useAncestrySubChoices(ancestryId)` + fonctions pures `getAncestrySubChoiceKeys`/`getAncestrySubChoiceRequirements`/`isAncestrySubChoicesCompleted` exposées pour validation côté submit. `AncestrySchema.options` étendu (Zod stripait jusque-là les options du bundle 13.7). 23 tests d'intégration disque→fonction pure.
- [x] 3. Triple gate verte (398 tests + lint + typecheck). Commit `84101bd` — `feat(form-kit): RadioCardGroup + useAncestrySubChoices (plan 13.8)`.

### Composants de sous-choix par ascendance (commit 2)

> Chaque composant rend un step intermédiaire entre « Ascendance » et « Caractéristiques » dans le wizard. Tous reposent sur `<RadioCardGroup>` (sauf `AncestryCastingAbilityChooser` qui peut être un `<RadioRow>` plus compact — décision tactique au moment de l'écriture).

- [x] 4. `dragon-ancestry-chooser.tsx` livré. 10 cartes (Noir/Bleu/.../Blanc) lues depuis `options.dragonAncestries`, impact = type de dégâts.
- [x] 5. `tiefling-legacy-chooser.tsx` — 3 héritages, description = cantrip L1, impact = résistance.
- [x] 6. `elf-lineage-chooser.tsx` — 3 lignages, description = bénéfice, impact = cantrip de lignage.
- [x] 7. `gnome-lineage-chooser.tsx` — 2 lignages, description = bénéfice, impact = cantrips fournis.
- [x] 8. `goliath-ancestry-chooser.tsx` — 6 ascendances gigantes, impact = effet mécanique (cadence PB/LR couverte en pédagogie commit 3).
- [x] 9. `ancestry-casting-ability-chooser.tsx` — INT/SAG/CHA, 3 cartes en colonne, description débutant pour chaque caractéristique. Composant unique réutilisé par Tieffelin/Elfe/Gnome.
- [x] 10. `ancestry-extra-skill-chooser.tsx` — props `ancestryId: 'elf' | 'human'`. Elf = 3 cartes (Perspicacité/Perception/Survie). Humain = 18 cartes lues depuis `human.options.skillfulOptions`. Layout 3 colonnes.
- [x] 11. `ancestry-size-chooser.tsx` — Petite/Moyenne avec impact mécanique vulgarisé.
- [x] 12. Wiring : `ancestry-sub-choices-section.tsx` (umbrella) rendu dans `ancestry-step.tsx`. Conditionnel selon `ancestryId` (Nain/Halfelin/Orc → ne rend rien). Pas de purge des sous-choix au changement d'ascendance (volontaire — l'utilisateur retrouve son choix s'il revient). `isAncestryValid` étendue : `isAncestrySubChoicesCompleted` bloque l'avance tant qu'un sous-choix requis reste en sentinelle. Pas de machinerie `subancestryId` à retirer (déjà nettoyé par 13.7).
- [x] 13. Triple gate verte (404 tests + 6 nouveaux tests wiring). Commit `558dd63` — `feat(wizard): ancestry sub-choice components (plan 13.8)`.

### Panneaux d'aide pédagogiques (commit 3)

> Pattern « 1 file `*-help.ts` par sous-choix », validé par Adrien.

- [ ] 14. `src/features/wizard/help/dragon-ancestry-help.ts` — pour chaque dragon : description simple (« un dragon de feu rouge, féroce et destructeur »), exemple de PJ célèbre (« comme Smaug »), impact mécanique en 1 phrase (« ton souffle inflige des dégâts de feu, et tu résistes au feu »).
- [ ] 15. `src/features/wizard/help/tiefling-legacy-help.ts` — pour chaque héritage : tagline FR, exemple narratif, mécaniques.
- [ ] 16. `src/features/wizard/help/elf-lineage-help.ts` — idem.
- [ ] 17. `src/features/wizard/help/gnome-lineage-help.ts` — idem.
- [ ] 18. `src/features/wizard/help/goliath-ancestry-help.ts` — idem.
- [ ] 19. `src/features/wizard/help/ancestry-casting-ability-help.ts` — explication débutant : « ta caractéristique d'incantation détermine la puissance de tes sorts. INT = savoir étudié, SAG = intuition, CHA = présence ».
- [ ] 20. `src/features/wizard/help/ancestry-extra-skill-help.ts` — idem skill picker.
- [ ] 21. `src/features/wizard/help/ancestry-size-help.ts` — Small vs Medium en termes mécaniques (slots de combat, armes lourdes, monture).
- [x] 14-21. 8 fichiers `*-help.ts` livrés (dragon, tiefling-legacy, elf-lineage, gnome-lineage, goliath-ancestry, ancestry-casting-ability, ancestry-extra-skill, ancestry-size). Pédagogie débutant FR : majuscule + ponctuation + zéro anglicisme. Garde de qualité rédactionnelle dans `__tests__/ancestry-sub-choice-help.test.ts` (101 tests : complétude par enum schema + qualité par entrée).
- [x] 22. Wiring : composant utilitaire `<ChooserHelpPanel>` partagé. Chaque chooser rend un panneau inline sous le `<RadioCardGroup>` avec le contenu de l'option courante. Pour caract.-incantation + skill + taille : panneau toujours affiché quand le chooser est visible (le sous-choix porte sa propre pédagogie indépendante de la sélection).
- [x] 23. Triple gate verte (505 tests). Commit `_pending_` — `feat(wizard): ancestry sub-choice help panels (plan 13.8)`.

### Validation wizard + submit (commit 4)

- [x] 24. Validation : `isAncestryValid` étendue (commit 2) consomme `isAncestrySubChoicesCompleted` ; nouveau helper pur `getMissingAncestrySubChoiceKeys(ancestryId, subChoices)` exposé pour piloter l'UI d'erreurs et la garde de submit.
- [x] 25. `submit-from-wizard.ts` peuple déjà `ancestrySubChoices: { ...draft.ancestrySubChoices }` (sentinelle pour Nain/Halfelin/Orc, valeurs pour les autres). Ajout d'une garde explicite au début de `buildCharacterFromWizard` : si un sous-choix requis reste null → throw avec message listant les clés manquantes. Filet redondant avec le bouton Suivant désactivé, mais protège contre bypass d'un draft persisté avant le wiring 13.8.
- [x] 26. `wizard-validation-ancestry-sub-choices.test.ts` — 20 tests couvrant chaque ascendance (Drakéide / Tieffelin / Elfe / Gnome / Goliath / Humain) en cas incomplet vs complet + ascendances simples (Nain/Halfelin/Orc) + state null. Couvre aussi `getMissingAncestrySubChoiceKeys`.
- [x] 27. Rouge-puis-vert : avant le wiring `isAncestrySubChoicesCompleted` dans `isAncestryValid` (commit 2), Drakéide sans `dragonAncestry` retournait `true` (juste `Boolean(ancestryId)`). Après wiring, retourne `false`. Test inverse rejoue le scénario : `{ ancestryId: 'dragonborn', ancestrySubChoices: empty } → invalid`. Vu rouge le temps du diff de commit 2, vert maintenant.
- [x] 28. Triple gate verte (525 tests + 20 nouveaux validation). Commit `_pending_` — `feat(wizard): ancestry sub-choices validation + submit guard (plan 13.8)`.

### Render dans Sheet — modes Combat et Magie (commit 5)

> **C'est la valeur utilisateur du plan.** Sans render dans la fiche, les sous-choix sont des données mortes.

- [ ] 29. **Souffle Drakéide en mode Combat** :
  - `src/features/sheet/modes/combat/attacks-list.tsx` ou composant équivalent : si `character.ancestryId === 'dragonborn'`, afficher une carte d'attaque « Souffle draconique » avec le type de dégâts résolu depuis `dragonAncestry` (cf. table audit A.2).
  - Le moteur de dés (`useDice`) doit pouvoir rouler le souffle (1d10 L1) → couplage avec `roll-with-flags.ts`.
  - Composant `breath-weapon-card.tsx` (nouveau) avec icône type de dégâts + DC du jet de sauvegarde (8 + Con + PB).
  - Affichage de la résistance dans le `status-strip` ou `essence-mode` (« Résistance : feu »).
- [ ] 30. **Sorts d'héritage Tieffelin en mode Magie** :
  - À la création (plan 13.8) : `submit-from-wizard.ts` inscrit le cantrip d'héritage dans `knownSpells['tiefling-legacy']` (nouvelle racine ou injection dans le canal natif — décision tactique, doc inline).
  - `magie-mode.tsx` lit ces sorts et les rend avec leur source visible (« Héritage Tieffelin »).
  - Caractéristique d'incantation utilisée = `character.ancestryCastingAbility`. Lue depuis le canal `spellcastingAbility['tiefling-legacy']` (mise à jour `submit-from-wizard.ts`).
  - Les sorts L3 et L5 sont inscrits mais grisés tant que le perso n'a pas atteint le niveau — composant ne casse pas, juste affiche un état « débloqué à L3 ». **Pas de bloc level-up ici** (c'est plan 18).
- [ ] 31. **Cantrips de lignage Elfe / Gnome en mode Magie** : même pattern que Tieffelin avec `spellcastingAbility['elven-lineage']` / `['gnomish-lineage']`.
- [ ] 32. **Goliath ascendance gigante en mode Combat** : carte de capacité dans le mode Combat avec compteur d'utilisations (PB par LR), tap → consomme un usage. Lecture du sous-choix `goliathAncestry` pour l'effet.
- [ ] 33. **Humain Skillful** : le skill choisi est posé dans `skills` du Character v2. Aucune UI dédiée — c'est juste un skill maîtrisé en plus, le render existant dans `essence-mode` suffit.
- [ ] 34. Triple gate verte. Commit : `feat(sheet): ancestry sub-choice renders — breath weapon, legacy spells, giant ancestry (plan 13.8)`.

### e2e Playwright (commit 6)

- [ ] 35. Étendre `tests/e2e/seed-character.ts` avec presets : `dragonbornL1Red` (souffle feu), `tieflingL1Infernal` (héritage feu), `elfL1Drow`, `gnomeL1Forest`, `goliathL1Storm`, `humanL1Skillful` (perception). Chaque preset alimente les champs schéma v2 attendus.
- [ ] 36. `tests/e2e/ancestry-dragonborn.spec.ts` : seed `dragonbornL1Red` → ouvrir /character/:id → tab Combat → assertion : une carte « Souffle draconique — Feu » est visible, avec DC visible.
- [ ] 37. `tests/e2e/ancestry-tiefling.spec.ts` : seed `tieflingL1Infernal` → tab Magie → assertion : `Trait de feu` apparaît dans la liste des sorts d'héritage.
- [ ] 38. `tests/e2e/ancestry-elf.spec.ts` : seed `elfL1Drow` → tab Magie → `Danses lumineuses` (Dancing Lights) visible.
- [ ] 39. `tests/e2e/ancestry-gnome.spec.ts` : seed `gnomeL1Forest` → tab Magie → `Illusion mineure` visible.
- [ ] 40. `tests/e2e/ancestry-goliath.spec.ts` : seed `goliathL1Storm` → tab Combat → carte « Tonnerre » visible avec compteur d'usages.
- [ ] 41. `tests/e2e/ancestry-creation-blocks.spec.ts` (test du blocage wizard) : aller dans /create, choisir Drakéide, tenter de passer à l'étape suivante sans choisir le type de dragon → l'étape ne change pas, message d'erreur visible. **Rouge avant fix.**
- [ ] 42. `pnpm test:e2e` vert (avec émulateur). Triple gate verte aussi.
- [ ] 43. Commit : `test(e2e): ancestry sub-choices L1 (plan 13.8)`.

### Documentation (commit 7)

- [ ] 44. `docs/AUDIT-SRD-COMPLETUDE.md` — section A : marquer chaque ligne « géré par wizard ? » à OUI avec référence au commit.
- [ ] 45. `docs/I18N.md` — vérifier qu'aucune nouvelle clé i18n ne contredit la convention (FR majuscule + ponctuation).
- [ ] 46. `plans/DEBT.md` — si un cas limite SRD émerge (cf. plan 13.7 § F.6 — un sous-choix non listé dans l'audit qu'on découvre au moment de coder), ne pas le ranger silencieusement : créer une entrée DEBT et arrêter pour validation Adrien.
- [ ] 47. Triple gate verte. Commit : `docs(srd): mark ancestry sub-choices delivered (plan 13.8)`.

## Definition of Done

- [ ] Tous les steps cochés.
- [ ] `pnpm typecheck && pnpm test && pnpm lint && pnpm test:e2e` verts.
- [ ] UAT navigateur (375 + 414 + 1024 px) sur chaque ascendance avec sous-choix : sélection forcée, panneau d'aide lisible, soumission produit une fiche v2 valide, render correct en Combat/Magie.
- [ ] Red-before-green vérifié sur chaque sous-choix (wizard refuse l'avance, sheet affiche le bon trait).
- [ ] `docs/AUDIT-SRD-COMPLETUDE.md` annoté.
- [ ] 7 commits conventionnels comme listés.

## Notes for next plan

- **Plan 13.9 démarre après 13.8 mergé.** Il reproduit le pattern par classe (Divine Order Clerc, Primal Order Druide, Fighting Style Guerrier, Weapon Mastery 5 classes, Expertise Roublard, Eldritch Invocations Warlock, langues, Wizard grimoire/préparés). Les composants form-kit `<RadioCardGroup>` et le hook utilitaire sont déjà livrés par 13.8 — 13.9 ne les réinvente pas.
- **Plan 13.10** (cleanup spells) peut être livré en parallèle de 13.9 si on veut paralléliser. Ne consomme rien de 13.8/13.9 ; régénère juste `spells.json` strict SRD.
- **Plan 13.11** : le format `grimwar-content-pack.json` côté ascendances doit accepter le champ `options.dragonAncestries[]` etc. que 13.7 a figé. Aucune divergence de structure entre SRD et custom.
- **Cas limites détectés à l'écriture de 13.8** : remonter ici toute découverte qui n'était pas dans l'audit (ex. un sous-choix d'ascendance qui interagit avec un champ qu'on n'avait pas anticipé). Ne pas patcher silencieusement.
