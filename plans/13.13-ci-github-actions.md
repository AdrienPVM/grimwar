# Plan 13.13 — CI GitHub Actions (câblage des 3 cadences livrées par 13.12)

> **Sprint** : S1 (outillage / qualité). **Statut** : en cours.
>
> **Pourquoi maintenant** : la matrice combinatoire L1 livrée par 13.12 (85 invariants sur 19 personas, garde-fou anti-dérive « axe matrice ≡ axe bundle ») est un **garde-fou en sursis** tant qu'elle ne s'exécute pas automatiquement. La première modification qui ajoute une 13ᵉ classe au bundle sans persona passera sans rien casser tant que personne ne lance `pnpm test:matrix` localement. Toute la valeur de 13.12 est conditionnée à ce plan. C'est le seul candidat qui **protège le travail déjà poussé** plutôt que d'en ajouter.

## Goal

Câbler en GitHub Actions les 3 cadences npm livrées par 13.12 + les gates de sécurité (rules) et e2e, sur un **flow mixte** : direct-push autorisé par défaut, branch protection bloquante sur les paths critiques (`public/data/**`, `scripts/data/srd-*.ts`, `.github/workflows/**`).

Livrable : un workflow `.github/workflows/ci.yml` à **4 jobs parallèles bloquants** (`static` / `unit` / `matrix` / `emulator`), validé par un **premier run réel sur PR draft** (un workflow ne se prouve pas vert par la gate locale — H1).

## Verdict d'inventaire (vérification des prémisses du cadrage 13.9 sur disque)

Discipline identique à 13.10/13.12 : chaque prémisse du cadrage oral (notes 13.9 + `tests/README.md`) confrontée au repo réel avant toute ligne. **Aucun stub `plans/13.13-*.md` n'existait** — 13.13 n'était cadré que dans les `## Notes` de 13.12 et les cross-refs de 13.14. Prémisses fausses/périmées :

- **P3 (split de cadence) — IMPRÉCISE puis tranchée nécessité, pas préférence.** `pnpm test` complet **n'est pas exécutable tel quel en CI** : il embarque `tests/firestore-rules.test.ts`, qui se **skippe silencieusement** sans `FIRESTORE_EMULATOR_HOST` (`firestore-rules.test.ts:37-44`) → couverture rules perdue sans bruit. La partition `fast ∪ matrix ∪ rules` couvre exactement tous les tests Vitest, chacun **une seule fois**. Le split Q1 est donc *structurellement nécessaire*.
- **P4 (anti-flake) — LARGEMENT DÉJÀ FAIT.** `workers:1` (`playwright.config.ts:46`) ✅ et `retries: CI?1:0` (`playwright.config.ts:42`) ✅ s'activent seuls car GitHub Actions exporte `CI=true`. La « quarantaine après 3 fails » n'a aucun équivalent natif Playwright (pas de mémoire cross-run) → **politique manuelle** (`test.fixme` + issue), pas de l'outillage. Ce volet se réduit à : exporter les artefacts trace/report on-failure (la config produit déjà les traces) + documenter la politique de quarantaine. → fondu dans les commits 3 (artefact) et 4 (doc).
- **P5 (« zéro secret = pas besoin de credentials ») — PARTIELLEMENT FAUSSE.** `env.ts:28-33` jette sur **toute** var `VITE_FIREBASE_*` requise vide (6 requises). Le `webServer` Playwright n'injecte que 2 vars (`PROJECT_ID`, `USE_FIREBASE_EMULATOR`) ; les autres viennent de `.env.local`, **absent en CI** → `pnpm dev` (serveur e2e) crashe au boot. Correctif : fournir **5 valeurs factices non-secrètes** en env clair du workflow (pas des GitHub Secrets). Option A tient (zéro secret réel), mais « pas besoin de credentials » sous-estime : il faut des placeholders de *présence*.
- **P6 (firebase-tools dispo) — FAUSSE (dépendance non déclarée).** `firebase-tools` n'est **pas** dans `package.json` (global sur le Mac d'Adrien). CI doit l'installer explicitement **+ setup Java** (`actions/setup-java`, l'émulateur en a besoin). Bonne nouvelle : `emulators:exec` **attend déjà** que l'émulateur soit prêt avant de lancer la commande — pas de healthcheck/sleep manuel à écrire, le piège « émulateur lent » est résolu structurellement par le script existant.
- **P7 (conflits de ports) — RISQUE FAIBLE.** Runner neuf = pas de conflit. `--only firestore,auth` ne démarre ni functions(5001) ni UI(4000). `--strictPort` sur 5179. Non-sujet sur un runner propre.
- **Constat Node** : `engines.node: ">=22 <26"`. La machine par défaut d'Adrien (v26) est **hors range supporté** → `setup-node@22` est le seul Node conforme, pas un confort. Gate locale par `nvm exec 22`.

## Décisions de cadrage (validées avec Adrien avant exécution)

- **Q1 — `test:fast` + `test:matrix` SÉPARÉS** sur chaque déclencheur (pas `pnpm test` complet). Signal fin (fast rouge = algo unitaire ; matrix rouge = couverture combinatoire) + parallélisme + cohérence avec les 3 cadences gravées en 13.12. Renforcé par la nécessité structurelle ci-dessus (P3).
- **Q2 — `test:rules` tourne en CI.** Les rules Firestore sont la sécurité du modèle de données ; une régression qui passe = trou de sécurité potentiel en prod. Coût marginal nul (l'émulateur tourne déjà pour les e2e). Satisfait trivialement la discipline « test:rules avant deploy rules ».
- **Arbre des jobs (plomberie tranchée par GSD)** : un seul workflow `ci.yml`, **4 jobs parallèles, tous bloquants**, triggers `push: main` + `pull_request`, `concurrency` + `cancel-in-progress`.

  | Job | Contenu | Émulateur/Java | Coût |
  |---|---|---|---|
  | `static` | typecheck + lint | non | léger |
  | `unit` | `test:fast` | non | léger |
  | `matrix` | `test:matrix` ← *le garde-fou de 13.12* | non | moyen |
  | `emulator` | `test:rules` puis `test:e2e` | oui (Java + firebase-tools + chromium) | lourd |

  - `unit` et `matrix` séparés → honore Q1 (signal fin + parallélisme).
  - `rules` + `e2e` groupés dans **un** job `emulator` → setup lourd (Java + CLI + browsers + jars) payé **une fois** ; la granularité Q1 est préservée par les statuts par-step de GitHub. `rules` tourne en premier (quelques s) puis `e2e`.
  - `static` isolé → typecheck/lint ne traînent jamais Java, feedback ~30 s.
  - Caching par job : pnpm store keyed sur `pnpm-lock.yaml` ; `emulator` ajoute `~/.cache/ms-playwright` (chromium) + `~/.cache/firebase/emulators` (jars).

- **H1 — branche `ci/13.13` + PR draft.** Un workflow ne se prouve PAS vert par la gate locale (ajouter un `.yml` ne touche ni typecheck, ni test, ni lint, ni e2e → gate verte trivialement, ne prouve rien sur le workflow). Le vrai « rouge avant vert » d'un workflow = son premier run réel sur GitHub. On observe les 4 jobs sur PR draft avant tout merge sur `main`. Version CI exacte du dispositif `uat-review/`.
- **H2 — « bloquant » est un réglage de repo, pas du fichier.** Le `ci.yml` *définit* les jobs ; les rendre requis pour merger = activer « required status checks » dans la branch protection GitHub (UI/API, action sortante d'Adrien). Liste des 4 checks à cocher fournie une fois le workflow vert : `static`, `unit`, `matrix`, `emulator`.
- **H3 — flow MIXTE.** Direct-push sur `main` autorisé par défaut. Paths protégés : `public/data/**`, `scripts/data/srd-*.ts`, `.github/workflows/**`. Justification : ce sont les bundles SRD durcis par 13.7-13.10b + la pipeline CI elle-même — une régression silencieuse y coûte cher et la CI seule en post-hoc serait trop tard. Pour les autres paths, le coût d'une PR systématique en solo dépasse le bénéfice. **À acter dans CLAUDE.md (nouvelle politique projet).**

  > **CORRECTION post-merge (2026-05-22) — l'hypothèse de mécanisme était fausse.** Au moment d'écrire la check-list de branch protection (étape 7), vérification de la doc GitHub : **branch protection et rulesets sont scopés à la branche, jamais conditionnés au path.** « Exiger une PR seulement si paths X » n'a **aucun mécanisme natif** : `Require a pull request` est tout-ou-rien sur la branche ; `Restrict file paths` bloque *absolument* (y compris les merges de PR) → inutilisable pour des bundles modifiés régulièrement par plan ; les `required status checks` ne mordent que sous flow PR. Auto-contradiction de surcroît : la justification gravée (« la CI post-hoc serait trop tard ») exclut le seul mécanisme natif compatible avec le direct-push (… une CI post-hoc). → **Voie A retenue** (cf. section dédiée + commit 5). La formulation CLAUDE.md a été reformulée en conséquence.

## Structure de commits (4, validée)

Ordre : squelette fonctionnel → cœur de valeur (garde-fou 13.12 verrouillé) → plomberie lourde émulateur → clôture docs. Le commit « anti-flake » du cadrage 13.9 a disparu (P4 = 80 % déjà fait) ; son contenu réel (artefact on-failure + politique quarantaine) est fondu dans les commits 3 et 4. Pas de commit fourre-tout.

- [x] **Commit 1 `8d1f822` — `chore(ci): workflow skeleton + job static + plan 13.13`.**
  Plan + `.github/workflows/ci.yml` : triggers `push: main` + `pull_request`, `concurrency` + `cancel-in-progress`, job **`static`** complet (checkout, `corepack enable` avant `setup-node@22` `cache: pnpm`, `install --frozen-lockfile`, typecheck, lint). Décision : `corepack enable` **avant** `setup-node` (le shim pnpm est activé sur le node système, puis `setup-node` localise le store via ce shim) — évite `pnpm/action-setup` (third-party) et matche l'instruction « corepack + pnpm@9.12.3 ». Setup inliné par job (pas de composite action) : ~5 lignes/job, bisectable, lisible. Gate Node 22 verte (trivialement — H1 : le `.yml` ne touche aucun test).

- [x] **Commit 2 `5c90a9e` — `ci: jobs unit + matrix parallèles (verrouille le garde-fou 13.12)`.**
  `unit` (`test:fast`) + `matrix` (`test:matrix`) parallèles, sans Java ni émulateur. Cœur de valeur : sans lui la matrice de 13.12 ne tourne jamais en CI. Gate Node 22 verte.

- [x] **Commit 3 `3393d73` — `ci: job emulator (rules + e2e) + Java + firebase-tools + env factices`.**
  Job `emulator` : `setup-java` Temurin 17, `firebase-tools@15.18.0` global (pinné sur la version locale d'Adrien → CI ≡ local ; non déclaré dans `package.json`, conforme au constat P6), cache jars émulateur + chromium, `playwright install --with-deps chromium`, **5 vars `VITE_FIREBASE_*` factices en env clair** au niveau job (P5 : `API_KEY`/`AUTH_DOMAIN`/`STORAGE_BUCKET`/`MESSAGING_SENDER_ID`/`APP_ID` — les 2 autres, `PROJECT_ID`+`USE_FIREBASE_EMULATOR`, sont injectées par le webServer Playwright), `test:rules` puis `test:e2e`, `upload-artifact if:failure()` du report Playwright (P4 réduit). YAML validé localement (ruby/psych). Gate Node 22 verte.

- [x] **Commit 4 `e685e04` — `docs(ci): badge README + section tests/README.md`.**
  Badge CI dans `README.md`. Section « Câblage CI » dans `tests/README.md` (mapping cadence→job + tableau « ce qu'un rouge signifie » + lecture d'échec + politique de quarantaine `test.fixme`+issue si ≥3/10 runs, jamais via retries). `test:nightly` reformulé (plus « à venir » : la matrice tourne déjà par-déclencheur via le job `matrix`). Flow mixte H3 + design CI actés en 2 lignes LOCKED du decision log CLAUDE.md. Clôture + Notes.

  > **Run réel (étape 6) :** PR draft #1 `ci/13.13`→`main`, **4 jobs verts au 1ᵉʳ run** (`static` 51s · `unit` 1m42 · `matrix` 25s · `emulator` 6m56 ; commit `0ac729d` = fix env factices + Java 21 trouvé au 1ᵉʳ run). Mergé fast-forward sur `main` (PR #1 « merged »). H1 a fait son travail.

- [x] **Commit 5 `512b8c1` (mergé via `eae03e6`) — `ci: protection double-couche paths critiques (flow mixte Voie A)`.**
  Implémente le flow mixte (H3) après l'échec de l'hypothèse branch-protection-par-path (cf. CORRECTION H3). **Voie A : mécanisme à 2 couches, du code livrable et testable, pas un réglage UI.**

  > **Amorçage par PR (dogfood, étapes 5-7) :** le commit installe le garde-fou et touche lui-même `ci.yml` (path protégé) → introduit **par PR #2 mergée avec merge commit** (`eae03e6`), pas en fast-forward, pour exercer le `--no-merges` du guard. Sur la branche : **4 jobs verts** (`static`/`unit`/`matrix`/`emulator`), `protected-paths-guard` **skipped** (correct, `if: push`). Sur le `push:main` consécutif au merge : **5 jobs verts dont `protected-paths-guard`** — il a vu le merge commit `eae03e6` comme seul commit first-parent, l'a exclu (merge), **passé vert** alors que `512b8c1` touche un path protégé. Auto-cohérence prouvée en réel : le mécanisme « les paths protégés passent par PR » est lui-même introduit par PR sans virer la CI au rouge.
  - `scripts/ci/protected-paths.sh` — matcher partagé, **source unique** des patterns protégés (regex `^(public/data/|scripts/data/srd-[^/]*\.ts$|\.github/workflows/)`). Reçoit des chemins sur stdin, exit 1 si match. Zéro dérive entre les 2 couches.
  - `.githooks/pre-push` (couche a, prévention locale) — refuse un push direct sur `main` touchant un path protégé. Auto-installé **sans husky** (pas de nouvelle dépendance) : `core.hooksPath .githooks` posé par le script `prepare` de `package.json`. Ne bloque que `main` (push de branche de feature → libre). Contournement conscient `--no-verify`.
  - `scripts/ci/guard-protected-paths.sh` + job `protected-paths-guard` dans `ci.yml` (couche b, détection ; `if: github.event_name == 'push'`, `fetch-depth: 0`) — échoue si un commit **direct non-merge** (`git rev-list --first-parent --no-merges BEFORE..AFTER`) touche un path protégé. Filet pour `--no-verify` + pushes hors-machine.
  - **Sémantique clé prouvée localement (worktree jetable) : un merge de PR (merge commit) est exclu** par `--first-parent --no-merges` → la PR est la voie légitime ; seul un commit direct déclenche le refus/échec. Rouge-avant-vert exécuté sur les 2 couches (matcher ×2 sens, hook bloque main+protégé / passe main+propre / passe branche-feature+protégé, guard fail direct / pass merge / pass propre / edge zero-sha).
  - **Bug latent corrigé en cours de test :** `… | "$HELPER" || true` masquait silencieusement un matcher introuvable (faux négatif). Les 2 couches échouent désormais LOUD (`exit 2`) si le matcher manque ; le guard résout son matcher via `BASH_SOURCE` (robuste au cwd/worktree).
  - Doc : `tests/README.md` (les 2 couches + bypass conscient) ; CLAUDE.md « Flow de développement » reformulé (mécanisme réel, hypothèse fausse documentée).

## Definition of Done

- [x] `plans/13.13-ci-github-actions.md` reflète l'exécution réelle (cases cochées, décisions inline).
- [x] `.github/workflows/ci.yml` : 5 jobs (4 parallèles + guard `if:push`), syntaxe valide, Node 22, pnpm@9.12.3, caching, env factices, artefacts on-failure. Prouvé vert sur PR #2 (branche) ET sur `push:main` (merge `eae03e6`).
- [x] Quadruple gate Node 22 verte. Pour le commit 5 : typecheck + lint locaux verts (le commit ne touche aucun TS app/test) ; **la gate complète — `static`+`unit`+`matrix`+`emulator` — a tourné verte en CI sur la branche `ci/13.13-guard` puis sur `push:main`**, ce qui est strictement plus probant que le local (H1).
- [x] PR draft ouverte, **4 jobs observés verts sur GitHub** (H1) — PR #1 (`ci/13.13`, base CI) puis PR #2 (`ci/13.13-guard`, garde-fou). La vraie « rouge avant vert » de la CI.
- [x] Badge + doc CI livrés.
- [x] CLAUDE.md : flow mixte H3 acté en politique projet (reformulé Voie A — hypothèse branch-protection-par-path écartée).
- [x] **Voie A livrée + testée en réel :** hook + guard. **Test (a) — fondateur, exécuté live :** `git push origin main` touchant `public/data/spells.json` **rejeté par le hook** avec message clair, zéro pollution distante. **Test (b) — `--no-verify` → guard CI rouge : NON exécuté live (décision Adrien Q2)** — la logique du guard est prouvée localement (worktree jetable, `--first-parent --no-merges`) ET en réel sur le `push:main` du merge (guard vert sur merge commit) ; un commit-poubelle revert sur `main` pour 3 mois était un mauvais ratio info/pollution. Câblage GitHub du guard (vars d'event + `fetch-depth:0`) validé indirectement par le run `push:main`.
- [ ] (Optionnel, action Adrien) **Required status checks laissés INFORMATIFS, pas `required` (décision Q3) :** sous direct-push ils ne mordent pas ; « required mais jamais enforced » crée un état UI confus. Le badge README donne déjà la visibilité. Réactivable si un flow PR plus large est adopté.

## Notes for next plan

**Ce que 13.13 livre :** une CI à 4 jobs (`static`/`unit`/`matrix`/`emulator`), poussée sur la branche `ci/13.13`, **à valider par un premier run réel sur PR draft** (H1 — un workflow ne se prouve pas vert localement). La valeur de 13.12 est verrouillée *dès que* le job `matrix` tourne vert sur GitHub.

**Flow mixte — Voie A (le mécanisme réel, cf. CORRECTION H3) :** la protection des paths critiques est livrée en **code** (hook `.githooks/pre-push` + job `protected-paths-guard`), pas en réglage GitHub. La branch protection native par path n'existe pas. Reste optionnel côté Adrien : activer les 4 required status checks comme indicateur visible (ne bloque rien sous direct-push).

**Pièges anticipés non encore prouvés (à surveiller au 1ᵉʳ run) :**
- Ordre `corepack enable` → `setup-node@v4 cache: pnpm` : recette canonique, mais si le cache pnpm rate (shim non résolu au moment du `cache:`), basculer sur `pnpm/action-setup@v4` avant `setup-node`. Plan B documenté.
- `playwright install --with-deps` fait du `apt-get` (non caché) à chaque run ; seuls les binaires navigateurs sont cachés (`~/.cache/ms-playwright`). Acceptable ; si lent, conditionner `--with-deps` au cache-miss.
- `firebase-tools@15.18.0` global : ~30-60 s par run (non caché par le store pnpm). Si gênant, le déplacer en devDependency (mais ça touche `package.json` → décision Adrien).

**Candidats prochains plans (rappel de fin de session précédente) :**
- **Mini-plan D18+D19** — content fix (thaumaturgie Tieffelin injectée + Fighting Style Défense +1 CA, découverts par la matrice). Léger, traçable.
- **Plan D1 dédié** — `spell.damage[]` (gros chantier extraction + moteur de calcul de dégâts de sort).
- **13.11** — custom content import (gros chantier UX).
- **13.14** — sheet responsive desktop (4 viewports, œil de design — UAT navigateur s'applique pleinement, contrairement à 13.13).
- **Mini-plan D15** — letter-spacing cleanup.
- **SRD-sourcing magic-items** (grandfathered AideDD aujourd'hui).
