# Plan 13.12 — Matrice de tests de parcours + Vérité du contenu

> **✅ LIVRÉ 2026-05-21 (5 commits).** La mécanique des 6 catégories « vérité du contenu » est en place : helpers content-truth fédérés, fixture SRD de référence (21 entrées), runner combinatoire L1 (19 personas → 85 invariants exécutés), garde-fou d'axe « matrice ≡ bundle » bidirectionnel (classes/backgrounds) + anti-fantôme (ancestries), 3 cadences npm (`test:fast`/`test:matrix`/`test:nightly`). 3 dettes découvertes par le runner et tracées (D1 bornée cat. 4, D18 thaumaturgie Tieffelin, D19 style Défense sans +1 CA). Reste : UAT navigateur Adrien (sensoriel) + push fast-forward. Bilan détaillé en `## Notes for next plan`.
>
> **Cadrage initial** : ce plan livre la **mécanique** (helpers réutilisables, fixtures, garde-fous, runner) qui rend systématiques les 6 catégories « vérité du contenu » déjà actées en politique transverse dans `CLAUDE.md > Required at every commit` depuis 2026-05-19.

## Goal

Rendre l'UAT manuel d'Adrien **l'exception, pas la norme** (mémoire `feedback_uat_manual_is_exception`). Tout ce qui est mécaniquement vérifiable — telle combinaison produit-elle un perso cohérent, telle donnée arrive-t-elle sur la fiche, tel calcul donne-t-il le bon nombre — relève des tests, pas d'Adrien.

Deux livrables conjoints :

1. **Helpers content-truth fédérés** (`tests/helpers/content-truth/`) — les primitives des 6 catégories « vérité du contenu », aujourd'hui éparpillées en tests standalone, extraites en helpers réutilisables sans casser l'existant.
2. **Runner combinatoire L1** (`tests/wizard-matrix/`) — driver la **vraie** fonction de prod `buildCharacterFromWizard` sur un espace de personas L1 généré par couverture (pas exhaustivité), avec garde-fou anti-dérive « axe matrice ≡ axe bundle ».

## Verdict d'inventaire (vérification des prémisses du stub sur disque)

Discipline identique à 13.10 : on a relu le stub et vérifié chaque prémisse contre le repo réel avant d'écrire une ligne. **7 prémisses fausses/périmées** trouvées :

- **B1** — `tests/fixtures/srd-reference-entries.ts` **existe déjà** (posé 13.9 commit 4b, 7 entrées : 3 sorts Magicien L1 + 4 Divine/Primal Orders). Travail réel = **étendre 7→20**, pas créer.
- **B2** — Cat. 3 (« Fidélité bundle ») est **déjà livrée mécaniquement** (`srd-reference-entries.test.ts` live et vert). Le plan la **fédère et l'étend**, ne l'invente pas.
- **B3** — Le factory de personnage de test **existe** : `buildCharacterFromWizard` (`src/features/wizard/submit-from-wizard.ts:109`). Async mais **pure** (n'appelle jamais `getDb` — seul `submitFromWizard` le fait), et `CharacterSchema` tolère les timestamps (`z.unknown()`). → Le runner **drive la fonction de prod réelle**, pas une réimplémentation. Point architectural majeur : beaucoup moins de code que le stub ne le laisse croire.
- **B4** — `reference-builds.test.ts` **sur-promet** : son entête dit « valide qu'un build produit un Character valide » mais le test ne vérifie que `pointBuy=27` + kebab-case. Le maillon « build → Character valide » n'est PAS testé. → Trou que le runner ferme (ROI immédiat).
- **B5** — Prérequis « 13.11 livré » **rétrogradé non bloquant** : 13.11 ajoute une source de contenu *côté campagne*, hors bundle public ; il ne change pas les axes SRD de la matrice. Branchement rétroactif sur l'outillage.
- **B6** — Axe Background = **4 figés** (set S1 verrouillé), pas « ×N à figer ».
- **B7** — Aucun engagement sur « 27 invariants » (chiffre venu du cadrage oral 13.9, pas du stub). Livrable = **6 catégories en helpers + runner + garde-fou d'axe**. Le nombre de cas est un résultat, pas une cible.

## Décisions de cadrage (validées avec Adrien avant exécution)

- **Q1 — Cat. 4 bornée** aux dégâts d'**arme** + CA + DD + mods + bonus de maîtrise + expertise (×2 pas ×3). **Dégâts de sort hors scope** : D1 (`spell.damage[]` non peuplé) reste ouverte, actée « rencontrée par cat. 4, non traitée » — ouvre la voie à un plan D1 dédié.
- **Q2 — 3 scripts npm distincts** : `test:fast` (Vitest invariants algorithmiques, <30s, par-commit) · `test:matrix` (Vitest matrice lourde, par-PR) · `test:nightly` (entry point **déclaré, non câblé** — 13.13 le branchera en GitHub Actions). Le découpage de cadence est une décision du plan matrice, pas du plan CI.
- **Combinatoire** : viser **couverture, pas exhaustivité**. ~1 persona représentatif par classe × variation d'axe ciblée qui change un calcul. Le compte est montré au STOP commit 3, pas fixé a priori. *(Principe gravé pour tout choix combinatoire futur.)*
- **Dettes à surveiller honnêtement** :
  - **D14** (marqueurs `[dette D14]` sur 4 sorts d'invocation) : allowlist documentée en tête de `expectIdentityRender`, les 4 sorts listés explicitement → acceptés tels quels (sinon faux-rouge).
  - **D15** (letter-spacing résiduel) : `expectIdentityRender` compare **après normalisation des espaces** — on détecte « modale du sort A affiche du contenu du sort B », pas « il manque un espace ». Si la normalisation cache un vrai défaut sémantique, on le remonte.

## Structure de commits (5, validée)

Ordre : primitives → application aux entrées connues → passage à l'échelle → protection anti-dérive → clôture. Chaque commit est gate-vert et bisectable. « Rouge avant vert » explicité pour chaque test anti-régression.

- [x] **Commit 1 — Fondations : helpers content-truth fédérés (C1 + C4 bornée).**
  Pose `tests/helpers/content-truth/` : `expectIdentityRender` (cat. 2, normalisation espaces + allowlist `[dette D14]`), `expectAC`/`expectSaveDC`/`expectAttackMod`/`expectProfBonus`/`expectExpertise` (cat. 4 bornée), `submitWizardAndDeriveSheet` (wrapper fin sur `buildCharacterFromWizard`, B3). Contrainte dure : ne casse aucun test existant. Rouge-avant-vert sur sonde temporaire (mauvais slug / mauvaise CA).
  - Livré : `normalize.ts` (stratégie D15 : `\s` JS couvre déjà U+00A0/U+202F), `identity.ts` (allowlist D14 sert de garde anti-marqueur-de-dette : échoue si un marqueur fuit sur un slug hors allowlist), `rules.ts` (compose les fonctions de prod `proficiencyBonus`/`computeAcFromArmor`/`abilityModifier`/`skillModifier` — pas de réimplémentation), `wizard.ts` (`submitWizardAndDeriveSheet` drive `buildCharacterFromWizard`, remonte un build invalide en `valid:false` plutôt qu'en exception), `index.ts` (barrel). Rouge-avant-vert prouvé en suite via 5 sondes `.toThrow()` (mauvais titre de modale, marqueur dette hors allowlist, CA fausse, DD faux, expertise ×3). Gate Node 22 : typecheck ✓ · lint ✓ · test 1026/9 skip ✓ · e2e 32/4 skip ✓.

- [ ] **Commit 2 — Cat. 3 fixture étendue 7→20 + fédération.**
  Étend `srd-reference-entries.ts` de 7→20 (B1/B2). 13 nouvelles entrées vérifiées **une fois** contre le bundle SRD (+ PDF FR si doute). Recâble `srd-reference-entries.test.ts` sur `expectIdentityRender`. **STOP data** : liste des 13 entrées + valeurs de référence soumise avant figeage. Rouge-avant-vert : muter une valeur → échec.

- [ ] **Commit 3 — Runner combinatoire L1 (C2 — cœur, ferme trou B4).**
  `tests/wizard-matrix/runner.ts` + `matrix.test.ts` : générateur de drafts L1 (par classe via `REFERENCE_BUILDS` × axes ascendance/background/variant + sous-choix L1, **borné par couverture**) → drive `buildCharacterFromWizard` → snapshot `{valid, knownSpells.count, ac, totalLevel, errors[]}`. Porte en unitaire les cas-limites cat. 6 (rogue-expertise, wizard-grimoire). **STOP data** : rapport de ce que la matrice attrape (D1 attendu, D14/D15 éventuels) + compte de personas, avant figeage des seuils. Rouge-avant-vert : draft volontairement invalide → `valid:false` + `errors[]` peuplé.

- [x] **Commit 4 — Garde-fou d'axe (C3) + 3 cadences npm (C5, Q2).**
  Assertion **axe matrice ≡ axe bundle** : clés de `REFERENCE_BUILDS` ≡ ids `classes.json` (idem ascendances/backgrounds). 13e classe ajoutée sans entrée matrice → **échec dur**. 3 scripts npm (`test:fast`/`test:matrix`/`test:nightly`). Rouge-avant-vert : fausse 13e classe → garde-fou échoue ; retirée → vert.
  - Livré : `axisDrift()` + axes exportés (`MATRIX_CLASS_AXIS` / `MATRIX_BASE_PERSONA_CLASS_AXIS` / `MATRIX_BACKGROUND_AXIS` / `MATRIX_ANCESTRY_AXIS`) dans `runner.ts` ; describe « garde-fou d'axe (C3) » dans `matrix.test.ts` (5 tests). **Deux régimes** : classes + backgrounds **bidirectionnels** (égalité exacte — set S1 backgrounds verrouillé à 4, tous couverts) ; ancestries en **subset** (la matrice couvre 5/9 ancestries ciblées par construction — coverage-not-exhaustivity ; garde-fou anti-fantôme seulement). **Écart documenté vs lettre « idem » du plan** : exiger l'égalité sur les ancestries forcerait l'exhaustivité, contraire au principe de cadrage — signalé au STOP. Ancien « 12 classes codées en dur » de `reference-builds.test.ts` (n'échouait pas sur dérive) **retiré et remplacé** par le garde-fou bundle-derivé. Rouge-avant-vert prouvé : injection réelle de `necromancer` dans `classes.json` → 3 tests rouges (`expected [ 'necromancer' ] to deeply equal []`), revert byte-identique → vert. 3 cadences npm câblées + `tests/README.md` documente le périmètre exact de chacune. Gate Node 22 : typecheck ✓ · lint ✓ · test **1125/9 skip** (matrice 80→**85**) ✓ · e2e **32/4 skip** ✓. `test:fast` chronométré **8,4 s** (< 30 s contractuel).

- [x] **Commit 5 — Clôture docs.**
  Acte D1 dans `DEBT.md` (rencontrée par cat. 4, bornage explicite). Note honnête D14/D15 selon ce que la matrice a remonté. Banner de clôture + `## Notes for next plan` (branchement rétroactif 13.11 ; câblage des 3 scripts par 13.13). Bilan réel du nombre d'invariants (refus de figer « 27 », B7).
  - Livré : D1 actée dans `DEBT.md` (« rencontrée par cat. 4 en 13.12, bornage dégâts d'arme + CA/DD/mods/expertise, ouvre la voie au plan D1 dédié post-13.12 », 0 faux-rouge). Note honnête sur D14 (runner L1 ne rencontre aucun marqueur `[dette D14]` — sorts niv. 2-5 hors espace L1 ; garde réelle = allowlist `DEBT_D14_SPELL_SLUGS` côté cat. 2) et D15 (`expectIdentityRender` normalise les espaces volontairement — masque D15, qui reste à traiter par sa passe dédiée). **Bilan d'invariants réel (refus B7 honoré, compté par grep, pas figé a priori)** : 8 helpers content-truth (`normalizeText`, `expectProfBonus`/`AC`/`SaveDC`/`AttackMod`/`Expertise`, `expectIdentityRender`, `submitWizardAndDeriveSheet`) + allowlist `DEBT_D14_SPELL_SLUGS` ; 21 entrées SRD de référence pinnées (13 sorts + 4 armes + 4 ordres divins/primordiaux) ; 19 personas → **85 invariants de parcours exécutés** (cohérence Zod, totalLevel, profBonus, baseAc, pins de sorts, cas-limites cat. 6, garde-fou d'axe, rouge-avant-vert) ; usages d'assertions content-truth à travers la suite : identity ×15, AC ×11, expertise ×8, profBonus ×8, attackMod ×7, saveDC ×7. **Découvertes du runner tracées** : D18 (thaumaturgie Tieffelin non injectée dans `knownSpells.ancestry`) + D19 (style de combat Défense sans +1 CA — sous-choix snapshot-invariant) ouvertes dès le commit 3, triggers de revisite documentés. Gate Node 22 (docs-only mais exécutée par principe).

## Definition of Done

- [x] Helpers content-truth/* livrés et testés rouge-puis-vert.
- [x] Entrées SRD reference pinnées et stables (**21** livrées : 13 sorts + 4 armes + 4 ordres ; la cible « 20 » du cadrage était indicative, B7).
- [x] Runner combinatoire L1 vert sur tous les personas générés (19 personas → 85 invariants).
- [x] Garde-fou d'axe « matrice ≡ bundle » en place et prouvé (rouge sur 13e classe fictive `necromancer`, revert byte-identique).
- [x] 3 scripts npm (`test:fast`/`test:matrix`/`test:nightly`) déclarés.
- [x] Quadruple gate (typecheck + test + lint + e2e) verte Node 22 à chaque commit.
- [x] D1 actée ; D14/D15 traitées honnêtement.

## Notes for next plan

- **Bilan d'invariants livrés (réel, compté — refus de figer « 27 » honoré, B7)** : la matrice grandit avec le système, le nombre est un **résultat**, pas une cible. À ce jour :
  - **8 helpers content-truth** (`tests/helpers/content-truth/`) : `normalizeText` (cat. 2/D15), `expectProfBonus` / `expectAC` / `expectSaveDC` / `expectAttackMod` / `expectExpertise` (cat. 4 bornée), `expectIdentityRender` (cat. 2 + garde anti-marqueur-de-dette via `DEBT_D14_SPELL_SLUGS`), `submitWizardAndDeriveSheet` (drive la vraie fonction de prod `buildCharacterFromWizard`).
  - **21 entrées SRD de référence pinnées** (`tests/fixtures/srd-reference-entries.ts`) : 13 sorts + 4 armes + 4 ordres divins/primordiaux, vérifiées une fois contre le bundle SRD (cat. 3).
  - **19 personas → 85 invariants de parcours exécutés** (`tests/wizard-matrix/`, run réel : `Tests 85 passed`) : 12 personas base (1/classe) + 4 axe ascendance + 3 axe background, plus cas-limites cat. 6, garde-fou d'axe (5 tests, 2 régimes), rouge-avant-vert.
  - Assertions content-truth réutilisées à travers la suite : identity ×15, AC ×11, expertise ×8, profBonus ×8, attackMod ×7, saveDC ×7.
- **Dettes découvertes par le runner (valeur de la matrice avant même son intégration CI)** :
  - **D18** — `knownSpells.ancestry` Tieffelin = 3 (triplet d'héritage) au lieu de 4 : la thaumaturgie commune « Présence d'outre-monde » n'est **pas injectée** par `buildAncestrySpellIds()`. Trou de **contenu**. Pin matrice documente le basculement attendu 3→4 / total 9→10 quand D18 sera résolue.
  - **D19** — style de combat **Défense** n'applique aucun +1 CA au build (sous-choix snapshot-invariant) : c'est la **raison documentée** pour laquelle la matrice ne teste pas les variations de style. Trigger de revisite gravé : le jour où `defense` devient snapshot-discriminant, on ajoute une persona de variation.
  - Principe transversal gravé : **on n'ajoute pas une persona/variation tant qu'elle est snapshot-invariante** par rapport à une déjà couverte. Trigger universel = un changement de code/contenu rend le cas snapshot-discriminant.
- **13.13 (CI)** câble les 3 scripts npm : `test:fast` obligatoire sur chaque PR, `test:matrix` sur PR ciblée, `test:nightly` (= `test:matrix` complet) sur push main / cron nightly. Le découpage existe déjà (`tests/README.md`, **interface définie, pas implémentation**) — 13.13 fait le wiring GitHub Actions, pas le redécoupage. `test:nightly` est aujourd'hui un **alias de `test:matrix` déclaré non câblé**.
- **13.11 (import custom)** se branche **rétroactivement** sur l'outillage matrice : un pack custom importé doit passer cat. 1/2/3 sur ses nouvelles entrées via les helpers content-truth livrés ici. La matrice s'écrit contre les bundles SRD, stables depuis 13.10 ; 13.11 réutilise les primitives sans les réinventer.
- **Plan D1 dédié** (post-13.12) : peupler `spell.damage[]` pour les 339 sorts (parsing prose, dégâts conditionnels + upcasts + types multiples, propagation moteur) puis élargir cat. 4 aux dégâts de sort. D1 est actée « rencontrée et bornée » par 13.12, pas traitée — voir `plans/DEBT.md > D1`.
