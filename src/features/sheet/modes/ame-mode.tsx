import { ModePlaceholder } from './mode-placeholder';

/** Mode Âme — TODO plan 20. Wrapper temporaire vers le placeholder partagé. */
export function AmeMode(): JSX.Element {
  return <ModePlaceholder mode="ame" />;
}
