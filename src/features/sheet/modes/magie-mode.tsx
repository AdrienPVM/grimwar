import { ModePlaceholder } from './mode-placeholder';

/** Mode Magie — TODO plan 09. Wrapper temporaire vers le placeholder partagé. */
export function MagieMode(): JSX.Element {
  return <ModePlaceholder mode="magie" />;
}
