import { ModePlaceholder } from './mode-placeholder';

/** Mode Essence — TODO plan 08. Wrapper temporaire vers le placeholder partagé. */
export function EssenceMode(): JSX.Element {
  return <ModePlaceholder mode="essence" />;
}
