import { ModePlaceholder } from './mode-placeholder';

/** Mode Avoir — TODO plan 10. Wrapper temporaire vers le placeholder partagé. */
export function AvoirMode(): JSX.Element {
  return <ModePlaceholder mode="avoir" />;
}
